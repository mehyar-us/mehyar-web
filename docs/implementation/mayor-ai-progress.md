# Mayor AI implementation progress

Source of truth: [complete goal](../plans/2026-09-16-mehyar-business-agent-goal.md) and [protected payment contracts](../plans/2026-09-16-product-payment-contracts.md).

Started 2026-09-16 on `codex/mayor-ai-platform`, based on `50e0c27990e9e27d5f487da8befc02b08d45ff4d`. The full A–Z objective remains unchanged and **in progress**. This ledger records partial implementation, not a reduced definition of completion.

## Current implementation

- Added completed-scan freshness with migration 0036. A stream timestamp advances atomically only with a final validated sync checkpoint, including empty scans; continuation pages preserve the previous timestamp and restart clears it. Status uses only current-consent streams and returns the oldest completion across configured folders, withholding it if any folder has never completed. The PWA separates scan completion from message observations and warns when a configured scan is over one hour old. This is a display threshold, not a measured latency/SLA or an automatic recovery claim. Throughput, push/watch enrollment, retention and live acceptance remain unfinished.

- Added customer mailbox recovery review/confirmation through the tenant Agent and authenticated mailbox/recovery endpoint. Expiring Durable Object offers bind user, connection, stream and expected round; responses expose an opaque offer ID and counts only. Reviews choose one affected stream at a time, reuse the same round offer and cap outstanding offers at ten per business. Execution rechecks expiry/binding around guarded recovery; completed retries reuse the receipt. The PWA explains replacement of pending references and revalidation of cached messages, requires explicit confirmation, retains the same offer after uncertainty and refreshes status after success. This is a recovery request, not proof of completed resynchronization. Per-folder naming/removal, sustained monitoring throughput and live acceptance remain unfinished.

- Added the guarded mailbox restart service and internal tenant Agent RPC. Migration 0035 binds recovery receipts to their original expected round. Current authorization and readiness are checked before work; Gmail captures a fresh validated profile history through guarded credential/provider access, while Graph restarts enumeration without a fabricated baseline. Completed request retries skip further provider reads and preserve later progress. Stop/consent changes during capture block reset; concurrent same-request results use the first committed receipt. Public recovery selection/confirmation and live provider evidence remain unfinished.

- Added an internal atomic mailbox restart primitive with migration 0034 receipts and cache reconciliation markers. It requires current authorization, a failed polling/consumer state, expected round and stable request key. Gmail receives a fresh caller-supplied history baseline and restarts bootstrap; Graph clears its delta checkpoint. One transaction records the request, starts a new round, fences old leases, retires superseded pending references and marks cached messages unverified. Re-observing a message clears its marker; status excludes unreconciled observations. Replayed requests cannot reset newer progress. This is internal only: provider-baseline capture/retry coordination, customer recovery controls and live evidence still need implementation.

- Added guarded account-level Resume monitoring. Migration 0033 adds a control revision: explicit stop advances it; resume accepts only the inspected stopped revision (or its already-completed retry) and cannot override a later stop. Resume requires current operator/account ownership, consent, paid/provider readiness, all processing flags and unpaused Agent authority, with SQL membership/consent/control predicates at commit. Status exposes resume eligibility and revision only while stopped; the PWA validates them, submits the revision and refreshes authoritative status. Resume preserves pending work, checkpoints, backoff and recovery states. Resynchronization and per-folder removal remain unfinished.

- Added per-account Stop monitoring through the tenant Agent/API and a customer confirmation control. Migration 0032 adds a separate mailbox_paused grant flag and atomically clears polling/consumer leases when stopped. Ledger authorization/SQL fences, provider setup guards and both dispatchers enforce it. Stop is repeatable and available to the current connection-owning operator without paid/provider readiness. It preserves credentials, other connector permissions, pending work and checkpoints; re-consent does not silently resume it. Status reports stopped and the UI refreshes after the action. Resume/resync and per-folder controls remain unfinished; the UI directs users to support before restarting.

- Added bounded historical mailbox consent review before provider processing. Migration 0031 records an indexed last-check time; each pass checks up to 25 due streams in oldest-check order. It recomputes authorization stamps without decrypting credentials and atomically retires mismatched pending work/leases while preserving current-consent streams. SQL predicates require the exact grant snapshot and saved stream stamp to remain unchanged at commit. Malformed/failed reviews get a bounded revisit delay without discarding their work. No provider flags or calls are required. Per-folder resync, retention, launch throughput and live acceptance remain unfinished.

- Added local mailbox maintenance before provider processing in the cron. It selects at most 25 streams with unauthorized grants, missing/expired/nonoperator membership, or offboarding/deleted tenants and outstanding work. Each D1 transaction rechecks current withdrawal before retiring poll/consumer leases and discarding pending references; valid paused workspaces retain their work. No execution flags, credentials or provider calls are needed for local cleanup. Failures roll back one stream and do not stop other candidates. Historical stale-consent detection for still-authorized grants, retention, automated recovery and full offboarding remain unfinished.

- Migration 0030 atomically retires pending mailbox references whenever a grant's consent revision, scopes or authorization status changes. A database trigger fences polling/consumer leases, leaves streams requiring review and marks pending references discarded, preserving cursors, receipts, already-applied references and cached messages. This prevents unusable old-consent pending work from exhausting the tenant queue after reauthorization. Token/ciphertext refresh alone does not trigger retirement. The migration also retires known currently unauthorized grants; historical stale-consent streams on still-authorized grants need separate bounded reconciliation. Membership/offboarding cleanup, retention and folder resync remain unfinished.

- Added persistent Outlook mailbox status through authenticated GET connections/:grantId/mailbox/folders/status and tenant-bound Agent RPC. Shared status logic retains Gmail behavior while aggregating only current-consent Outlook streams: configured folder count, pending reference count, latest message observation, initialization and recovery state. Microsoft initialization remains pending until each selected folder has a final delta checkpoint. Counts expose no folder IDs, cursors or message contents. The PWA displays saved Outlook status, supports refresh, validates counts and clears stale data on invalid/failed/offline reads. Per-folder removal/resync, freshness guarantees and live acceptance remain unfinished.

- Added Outlook folder setup through authenticated tenant POST connections/:grantId/mailbox/setup and owning Agent RPC. Only one to 100 distinct folders from a complete, unexpired, current-consent inventory can be configured. All mailbox flags/readiness and current grant-owning operator checks apply. A single fenced D1 INSERT adds the entire selection atomically; deterministic stream IDs make repeats preserve existing cursors, pending work and recovery states. The PWA enables checkboxes only after complete discovery, freezes selection after uncertain setup and retries the identical payload. Setup is additive; it does not remove existing folders, reset recovery or authorize sending. Result copy distinguishes saved configuration from completed synchronization. Persistent Outlook status, removal/resync controls and live provider evidence remain unfinished.

- Added the customer Outlook folder-discovery panel under eligible Microsoft connections. Explicit discovery/continuation/reload actions call the tenant API; validated inventories show folder names, parent labels and hidden status. Incomplete traversal is never presented as complete. Offline, failed and invalid responses clear previously shown data; expired handles require reloading. The panel states that discovery does not start monitoring. Folder selection and synchronization setup still need implementation; provider release flags remain false.

- Wired Outlook folder inventory into BusinessAgent initialization/RPC and authenticated POST connections/:grantId/mailbox/folders with strict optional UUID continuation input. The service requires the current grant-owning operator, paid mailbox readiness, Microsoft capability/release gates and live Agent authority. It binds inventory to current consent, guards credential refresh and each provider request, and rechecks after provider reads. Stale or foreign inventory handles are rejected before credential refresh. All mailbox flags remain false. Folder selection/setup activation and the customer picker are still pending; the public service has no live-provider acceptance evidence.

- Added a durable Outlook folder-inventory session component, tested against real Durable Object SQLite storage. It follows root/child pagination in five-page batches, retains private cursors, binds rotating handles to user/grant/consent, uses expiring work leases and ten-minute sessions, and checks authority around every provider read. Traversal stops explicitly incomplete on duplicate resources, loops or 100 pages; inventory storage is capped at 10,000 folders/1 MB. Only completed inventories permit selection of one to 100 distinct known folder IDs. This component is not yet wired into Agent RPC, HTTP or customer setup; integration and live provider proof remain required.

- Added Microsoft Graph root/child mailbox folder discovery with hidden-folder inclusion, explicit fields and server-side pagination. Response validation rejects malformed folder records, duplicate IDs, self-parenting, wrong-parent children and continuation links outside the exact requested collection. This is the provider adapter only; durable tenant-scoped inventory, folder picker and Outlook setup remain unfinished.

- Added authenticated Google mailbox GET/POST routes under the current tenant connection and a Connections-panel setup/status flow. Only the grant-owning operator can read it; membership, tenant and current consent identity are checked again after the read. Status exposes setup state, pending count and latest observation only, never stream IDs/cursors/message contents. New setup is offered only with current paid/provider readiness and all three mailbox flags; POST uses the existing idempotent initializer and returns sanitized status. Old-consent progress is not shown as current. The PWA clears offline/failed/invalid status, refreshes after uncertain setup and distinguishes configured monitoring from evidence of message handling. Gmail only is exposed; Microsoft folder selection/setup, freshness monitoring, watch enrollment, classification, retention and live acceptance remain unfinished.

- Added recurring mailbox processing before new mailbox polling in the new-platform cron. All three mailbox flags must be true before selection or Agent RPC. The dispatcher chooses at most five eligible tenants and one stream each, then rotates through at most five reconciliations per tenant (25 total). Processing eligibility uses pending work and consumer leases/retry state independently of polling cursor state, due time and backlog cap, so a full or resync-required queue can drain. Each dispatch uses the owning Agent consumer and its live guards. Failed tenants stop their batch without stopping others; unchanged idle failures get a bounded delay, while active claims, provider backoff and concurrent progress are preserved. All flags remain false. The dispatcher budget is bounded scaffolding, not verified production throughput; scaling/latency, customer onboarding, watch intake, classification, retention and live acceptance remain unfinished.

- Migration 0029 adds a separately leased pending-reference consumer and private, resource-scoped current-message snapshots. The consumer rechecks actual subscription/provider/grant/Agent authority around current Gmail message reads or folder-scoped Outlook reads, even for deletion hints. Validated provider IDs must match the requested message; only a current 404 records scoped absence. Snapshot persistence, pending-reference acknowledgement and lease release commit atomically; stale/revoked workers cannot acknowledge work. Bootstrap/incremental/unknown origin is retained. Content is untrusted and bounded to 128,000 bytes per message, 10,000,000 content bytes and 10,000 snapshot records per tenant. Failures back off or require review, with twelve attempts maximum. MAILBOX_PROCESSING_ENABLED defaults false; internal Agent RPC exists, but processing is not yet scheduled. Classification/drafting, customer inbox, body extraction/attachments, retention/export/deletion, operator recovery and live provider tests remain unfinished.

- Migration 0027 adds a durable Gmail bootstrap phase. Internal initialization checks paid/provider/Agent/grant authority, verifies the Gmail profile against the connected mailbox, captures its history ID before enumeration and creates a consent-bound bootstrap stream. Repeated initialization returns existing progress without a fresh profile request or reset. The normal gated runner pages message references, including spam/trash to match mailbox-wide history, and atomically changes to incremental mode on the final page while retaining the original baseline. Immediate history catch-up can then collect changes made during enumeration. Malformed profile/list responses fail closed, and a bootstrap cannot advance to a different baseline. This enumerates references only: message retrieval/processing, public onboarding integration, watch setup and full resync recovery remain unfinished.

- Added independently gated recurring mailbox dispatch to the existing new-platform 15-minute cron. With both mailbox flags enabled, it selects at most five due streams and one per tenant, excluding suspended/revoked/leased/not-due/backlogged work. It calls syncMailbox on the correctly named BusinessAgent, where tenant binding, operator membership and live Agent pause are checked around the composed provider runner. Per-tenant failures do not stop other dispatches; unchanged idle failures receive a 15-minute delay without replacing concurrent progress, provider backoff or live claims. The dispatcher and runner remain disabled by default. Stream onboarding/bootstrap, watch enrollment, pending-reference processing, retired-consent cleanup and live provider evidence remain unfinished.

- Migration 0026 adds durable next-poll timing and consecutive attempt accounting. Completed mailbox rounds wait five minutes; continuation pages remain eligible immediately. Transient failure delays now use next_poll_at independently of worker leases. Twelve failed/abandoned attempts require explicit recovery; only an atomically saved page resets the count. Claims require room for a full 1,000-reference page, and receipt insertion atomically caps pending references at 10,000 across all streams of one tenant, including concurrent streams. Capacity rejection cannot save a receipt or advance a checkpoint, and processing existing pending work restores capacity. Watch-triggered wakeups, recurring dispatch, pending-reference consumers, explicit recovery and retention remain unfinished.

- Added an internal one-page Gmail/Outlook runner composing the typed adapters, renewable credential custody and durable mailbox ledger. It checks paid access/catalog activation, actual grant authorization, configured read capability, provider evidence and a required live Agent pause guard before provider requests and before persistence. Gmail history references are deduplicated within each page; Outlook removal references stay scoped to their folder. Only minimal message IDs/kinds are queued, never mailbox bodies. Invalid/expired cursors require resynchronization; rate limits and transient read failures retain the checkpoint and persist backoff without immediate retry. MAILBOX_SYNC_ENABLED defaults false. No public or recurring caller, live bootstrap/watch setup, consumer execution, retention or provider acceptance proof is included yet.

- Migration 0025 adds an internal durable mailbox synchronization ledger. Streams bind tenant, grant, provider, folder and consent identity; current grant-owner membership, read permission and tenant state are checked before work, with SQL authority fences on mutations. Ninety-second leases reject concurrent/stale callbacks. One D1 transaction saves a page receipt, minimal pending message-change references and its continuation/final checkpoint; exact replay cannot duplicate a page, and storage failure cannot advance its cursor. Cursor loops and a 500-page round bound require recovery; expired checkpoints remain preserved for resynchronization. No public route, recurring runner, full mailbox bootstrap, pending-change executor, watch enrollment, provider call or production activation is added by this storage component.

- Added runtime validation for Gmail history and Outlook delta pages before exposing changes or checkpoints. Gmail IDs retain full integer precision and invalid/regressing history is rejected; Outlook requires exactly one continuation/checkpoint link bound to the requested folder and preserves deletion tombstones and partial updates. Eighteen fixture cases cover malformed pages, foreign links, large IDs and final-page checkpointing. Durable mailbox checkpoint storage, watch recovery, recurring intake and live provider evidence remain unfinished.

- Added local-only email maintenance before provider work in the new-platform cron. It shares the outbox's actual authority predicate, handles up to 25 invalid or incompletely reconciled jobs, and does not depend on any sending flag or credential. Expired cancelled leases become review-required while uncertainty stays reserved; cancelled unattempted jobs release capacity; saved acceptance repairs interrupted customer-capacity settlement. Active leases and healthy paused jobs remain intact. No fresh send claim, network call or invitation-access mutation occurs. Operator resolution of uncertain effects and supplier invoice reconciliation remain unfinished.

- Migration 0024 adds durable owner-requested invitation-email cancellation without revoking the invitation. The scoped cancel endpoint remains available while delivery/billing/workspace operation is paused, requires current ownership and records one audit entry. Claims/final dispatch checks reject cancellation requests; unattempted jobs cancel and release capacity, active leases remain available for historical outcomes, and potentially submitted work stops future attempts while retaining uncertainty/commitments. The PWA offers cancellation independently of the sending flag and distinguishes an in-flight request from confirmed unattempted cancellation or unknown earlier delivery. Already accepted provider outcomes remain accepted; no mail recall is claimed.

- Added a separately disabled Resend event endpoint at /api/platform-email/webhook and durable inbox migration 0023. Raw UTF-8 bytes are bounded before HMAC-SHA256 verification of Svix ID/timestamp/body, with five-minute delivery tolerance and rotation signature support. Supported delivered/bounced/complained hints are deduplicated by event ID and exact payload hash; conflicting reuse fails. Recovery links at most five hints to uniquely matched accepted jobs under the original verified sender route, waking capped receipt polling without claiming delivery or suppression from the hint itself. Early unmatched events retry hourly up to 48 times, ambiguous mappings require review. No secret/endpoint enrollment, real event replay or production activation occurred; operator/event-history recovery and live evidence remain unfinished.

- Owners and billing members can now read a tenant-scoped platform-email usage snapshot and view it in Billing. It separates accepted capacity from held/uncertain reservations, excludes released slots, reports remaining allowance and monthly reset time, and warns at 70%, 90% and 100%. Subscription authority is checked before/after the count, membership before/after the read; unavailable entitlement is not represented as a verified zero balance. The PWA clears figures after read failures/offline changes, rejects invalid counts and exposes no subscription/provider IDs. This is included-plan capacity, not a supplier invoice or new customer charge; purchased email packs/top-ups remain unfinished.

- Owners can explicitly request an invitation email through the scoped POST team/invitations/:id/email endpoint. It requires current ownership, both email flags, verified paid access and sender readiness; it queues one immutable invitation job and never sends within the request. Team directory responses now include sanitized delivery state/check time and flag availability, without provider IDs, credentials or payloads. The PWA offers a queue button only for pending unqueued invitations when enabled, safely retries the same invitation after uncertain responses, and distinguishes provider acceptance from recipient-server delivery. Both runtime flags remain false; no real messages or deployment occurred. Signed webhooks, operator support/recovery controls and live provider acceptance remain unfinished.

- The new-platform 15-minute cron now invokes independently gated email recovery. AGENT_PLATFORM_EMAIL_RECOVERY_ENABLED and the sender flag both default false and are checked before configuration/database work. A tick performs at most five receipt checks then five send candidates, one per tenant per batch, with per-job leases protecting duplicates. Blocked unleased send candidates receive a 15-minute scheduling delay without consuming provider attempts or replacing their job/key; changed leases/backoff/terminal states are not overwritten. Paused workspaces do not enter send batches. No production activation or external messages occurred. Public preparation/UI, signed webhook intake, operator recovery and live provider evidence remain unfinished.

- Migration 0022 adds internal delivery receipt polling for known accepted invitation jobs. Reads bind the original sender configuration, tenant, provider ID and exact stored payload; foreign/changed routes make no request. A per-job lease, hourly successful checks, 15-minute failed-check backoff and 48-check ceiling bound polling. Confirmed delivery/bounce/complaint observations persist across later last-event changes; opened/clicked do not substitute for delivery proof. Verified new bounce/complaint facts atomically add global platform recipient suppression, once per fact so repeated polling cannot undo an operator release. No public/scheduled caller or live provider request exists. Signed webhook intake, long-term recovery, operator controls, customer delivery UI and live evidence remain unfinished.

- Added internal dispatchInvitationEmail to connect verified claims, final authority/allowance/supplier checks, the fixed Resend transport and durable outcome settlement. Each invocation snapshots credentials, makes at most one provider request and preserves job payload/key/first-attempt time for later recovery. Disabled configuration fails before transport; denied final checks record review/deferred state. Historical acceptance settles even if readiness is withdrawn during transport. Lost settlement is reported separately or as a sanitized persistence failure, never silently retried with a new job. There is no public or scheduled caller, production activation or real email send. Delivery receipt reconciliation, recovery scheduling, user-facing state and live provider tests remain unfinished.

- Migration 0021 adds operator-owned supplier account budgets, verified configuration-to-account mappings and immutable per-job cost commitments. Verified invitation claims atomically reserve against the shared account window before claiming an attempt, and dispatch rechecks capacity, evidence, cost estimate and account mapping. Evidence expires within 30 days; missing/revoked/expired records fail closed. Commitments use integer USD micro-units and must conservatively cover all five possible attempts and variable fees. Only cancellation before any attempt automatically releases cost; accepted or uncertain jobs retain it pending actual supplier reconciliation. No real account/cost approval is seeded. Supplier bill reconciliation, fixed/shared-account costs, scheduling, operator controls and live delivery remain unfinished.

- Verified invitation claims now resolve platform-email capacity from the current subscription/catalog and reserve it before an outbox attempt. Dispatch eligibility rechecks subscription access, activation evidence, monthly window and held capacity alongside sender and invitation authority. Catalog defaults are 1,000/3,000/10,000; annual billing still uses monthly anniversary windows. Trial, mismatched/obsolete/disputed subscription records and expired access fail closed. Historical acceptance consumes reservations after authority withdrawal. Add-on entitlements, supplier account budgets, customer metering, dispatch scheduling and live delivery remain unfinished; no Stripe code or configuration changed.

- Migration 0020 and PlatformEmailAllowance add an internal atomic capacity ledger for platform email jobs. One job reserves one immutable monthly period; concurrent reservations respect the supplied server allowance, retries reuse capacity, uncertain results remain held, acceptance consumes capacity and only proven cancellation before any attempt releases it. Reduced limits and expired windows fail eligibility checks; cancelled jobs cannot revive their reservation. This is not customer billing or live enforcement: authoritative paid-subscription allowance resolution, dispatcher integration, add-on entitlements, supplier budgets, metering UI and live proof remain unfinished. No public API accepts allowance inputs.

- Migration 0019 adds platform-only recipient suppression with global or workspace scope, explicit reason/status and operator evidence. Outbox claim SQL and final dispatch eligibility enforce active blocks independently of sender credential/route. Reconciliation cancels never-attempted blocked mail, moves potentially submitted mail to review, preserves active leases for historical settlement, and never revives cancelled jobs on suppression release. Provider-event ingestion, operator management, quotas, supplier commitments and live dispatch remain unfinished; no legacy sender reads this table.

- VerifiedInvitationOutbox now composes readiness checks with durable invitation jobs. Preparation and claiming bind the full verified configuration hash, preventing an unchanged human route label from moving an existing job to a different credential or sender. Dispatch eligibility rechecks current evidence and invitation authority; historical accepted outcomes can still settle after evidence is withdrawn. No API or scheduler invokes transport, and runtime suppression, quotas, supplier commitments, delivery recovery and live evidence remain unfinished.

- New platform email configuration now has a disabled-by-default flag and dedicated sender/route/credential fields. Migration 0018 adds operator-owned readiness records bound to a hash of catalog, environment, HTTPS app origin, sender and credential fingerprint. Required gates cover verified domain/credential scope, delivery recovery, suppression and supplier cost limits; missing/revoked/empty/expired/future/overlong evidence fails closed. No real approvals or credentials were seeded and no public evidence writer exists. This is a configuration release gate, not proof of provider approval or runtime suppression/quota enforcement. Dispatcher integration and live evidence remain unfinished; migration applied locally only.

- Internal invitation-email authority reconciliation now terminates unattempted invalid work as cancelled and routes potentially submitted work to review_required after its lease expires. Temporary workspace pauses retain resumable state. Claiming invokes reconciliation, and mayDispatch verifies current permission, lease, routing, retry window and exact frozen payload/key/time immediately before future transport use. Live sender wiring, scheduled sweeps, quotas, provider receipt reconciliation and operator resolution remain unfinished. No external sends or new deployment/migration were performed.

- Added durable internal invitation-email preparation, lease claims and result settlement in migration 0017. Message/hash/key/sender route and first-attempt time remain immutable across retries and restarts. Claims enforce current invitation/owner/workspace conditions and exclude changed routing; stale workers cannot overwrite newer results. Accepted is distinct from delivered, retries back off and stop at five claims/23 hours, and later rejections preserve uncertainty as review_required. No API or scheduler activates this module yet. Sender/account verification, suppression, tenant/account quotas and supplier commitments, final dispatch checks, cleanup, delivery events, operations and live proof remain unfinished. Migration applied locally only; legacy flows remain untouched.

- Added the specified Resend platform-notice transport and fixed plain-text invitation template. This is an unconnected foundation, not completed email delivery: no Worker route or scheduler imports it and no live credentials were added. It bounds response size/time, uses a namespaced idempotency key, makes no automatic retries, refuses stale/future first-attempt timestamps, distinguishes acceptance/uncertainty/rejection/conflict/rate limiting and validates exact stored payload identity on receipt reads. The durable outbox, frozen payloads and first-attempt state, sender/account gates, recipient authority, suppression, quotas, scheduling, delivery webhook and live proof remain required before activation. Existing product senders are untouched.

- Recipient invitation pagination now reaches beyond the first 50 pending invitations. Each cursor is resolved against the signed-in account's current verified email; accepted/revoked anchors remain usable as positions without restoring invitation authority. Returned entries still enforce pending status, expiry, workspace lifecycle and inviter ownership. The inbox appends distinct entries, refreshes to discover new invitations and clears private data on failed continuation. Migration 0016 applied locally only. Invitation email delivery and the wider live provider acceptance matrix remain open.

- Owner Team directory pagination now reaches beyond the former 100-member/50-invitation limits. Independent scoped cursors and timestamp/ID ordering avoid gaps for tied timestamps. Owner authority is rechecked after page reads; foreign anchors fail. The PWA appends each list independently, deduplicates IDs, resets on refresh and clears private data on continuation failures. Additive migration 0015 supplies ordering indexes and is applied locally only. Recipient invitation-inbox pagination, invitation email delivery, owner transfer and temporary support management remain open.

- Member removal now uses an explicit expected membership revision and immutable idempotency receipt. Migration 0014 adds removal receipts; conditional receipt creation, revocation and audit commit atomically. A repeated old removal returns its recorded result without revoking a reinvited member; reused keys with changed payloads and stale/new competing changes fail. The owner confirmation keeps its original key/revision after an uncertain response and refreshes current membership after success. All 290 service tests in 19 files and 67 browser tests passed; builds, TypeScript and the 503-file legacy guard passed. Migration applied locally only. This closes the previously recorded removal-retry gap; remote provider token revocation and broader live recovery remain unverified.

- Owners can now explicitly change active ordinary member roles between manager, staff, billing and viewer. Migration 0013 adds membership revisions and durable role-change receipts. Conditional receipt creation, mutation and audit commit atomically; exact retries return the original recorded result without rewriting newer roles, and removal/reinvitation advances revisions. Ownership, temporary support roles, revoked/expired memberships and foreign targets are excluded. The Team editor preserves uncertain request payloads, requires refresh after conflicts and reloads current membership after a confirmed save. All 288 service tests in 19 files and 66 browser tests passed, plus both builds, TypeScript, accessibility and the legacy guard. Migration applied locally only. The removal-retry follow-up identified here is now implemented in migration 0014 (see latest entry).

- Owner Team management now lists memberships and invitations, creates idempotent seven-day invitations for manager/staff/billing/viewer roles, revokes pending invitations and removes non-owner access with recorded actor/time. Recipients see pending invitations after signing in with the exact invited verified account email; acceptance atomically rechecks identity, inviter ownership, workspace state and catalog seats before creating membership. No credentials are copied. Concurrent accepts cannot overfill the final seat, and replaying an accepted invitation cannot restore revoked membership. Unknown/trial plans allow only the existing owner seat until a catalog plan is assigned; no trial team-seat entitlement has been added. The directory shows up to 100 members and latest 50 invitations with explicit truncation; full directory pagination, role editing, owner transfer and support workflows remain open. Email invitations are not sent yet, and the UI states the manual sign-in handoff. Migration 0012 applied locally only. Full verification: 286 service tests in 19 files, 64 browser tests, TypeScript, frontend build, Worker dry-run and the 503-file legacy guard passed. No production deployment or external messages.

- Billing activity now exposes server-recorded setup, payment-failure/action-required, renewal, cancellation, refund, dispute and credit notices to current owners/billing members. Migration 0011 adds random public pagination keys and backfills existing notices without exposing provider object/event IDs. The API returns 25 records per page, checks membership before/after reads, validates cursors within the selected business and does not mark notices as emailed. The PWA supports older entries, refresh and offline/error/workspace clearing. Full runs passed 283 service tests in 18 files and 61 browser tests, including accessibility and tied-timestamp/cross-business pagination cases. TypeScript, frontend build, Worker dry-run and the 503-file legacy guard passed. Migration applied locally only; no production effects. Email delivery, delivery receipts and broader notification preferences remain incomplete.

- Owners and billing members can now see their workspace's latest billing snapshot check, plain-language discrepancies, completion time and stale/failed states. Reports omit internal errors and provider identifiers, remain hidden before any check exists, and explicitly cover only the subscription/latest invoice. This is diagnostic visibility, not financial certification or automatic missed-event repair.

- Recommendations now use an explicit owner-selected industry pack from the ten catalog packs, with pack prerequisites, catalog plan/setup prices and tailored preview examples. Clinics remain restricted to public FAQ/non-patient setup suggestions. The editor exposes a saved selector; old briefs and receipts normalize compatibly. All-pack and selection fixtures plus the browser retry flow cover this behavior. Industry-specific question collection and conversational personalization remain unfinished.

- Brief revisions now preserve server-verified source snapshots per field. New saves reject changed/deleted/foreign source claims; original receipts replay without overwriting newer revisions even after source deletion. The editor carries source IDs, shows saved links and drops attribution on manual edits. Browser provenance flow passes alongside the 50-test suite; no action permissions are inferred.

- Confirmed research now supplies bounded brief suggestions with original URL, retrieval/confirmation timestamps and confidence. Suggestions require matching current owner-confirmed memory and exclude deleted/edited facts, foreign tenants and operating-authority claims. Owners explicitly copy into empty draft fields and must separately review/save; current text is never overwritten. Service and browser cases pass (255 service, 50 browser); saved per-field provenance is now implemented; conversational collection remains unfinished.

- Business Knowledge now includes the business-brief editor and three proposed recommendation cards. Owners review and save; managers read only. Exact uncertain retries, conflict refresh, catalog plan/setup pricing, unresolved questions, offline handling and tenant-keyed state are implemented. Two browser cases plus the full 48-test suite pass; the new panel passes accessibility checks and its mobile fixture capture was visually reviewed. Research-to-brief integration remains unfinished.

- Added a private per-business owner-reviewed brief API with bounded structured fields, revision-conflict protection, immutable retry receipts and owner/manager read isolation. It returns three proposed catalog-backed recommendations (prices/prerequisites/examples) and only unanswered operating questions. Tests cover concurrent edits, stale retries, proposed-only recommendations and tenant/role revocation. The customer editor is now implemented; source integration, industry-aware ranking and conversational onboarding remain unfinished.

- Disabling research now stops queued/running work locally on maintenance independently of recovery configuration. Definitive requester/entitlement loss requests cancellation before further provider reads, while readiness/infrastructure failures retain state for retry. New fixtures distinguish missing readiness from membership revocation and verify disabled execution transitions without external calls. Remote stop completion still requires gated recovery and verified terminal evidence.

- Pause now atomically stops queued research, marks in-flight submission uncertain and requests cancellation of known running jobs. Enabled recovery is scheduled without provider I/O in the pause request, and resume does not restart cancelled work. A local Agent callback integration fixture covers startup recovery, actual-adapter synthetic DELETE/GET responses, settlement and idle schedule removal. Deployed timing and live provider acceptance remain outstanding.

- Agent maintenance is now connected to research coordination through the persisted Agent identity. Separate disabled execution/recovery switches, server-only account/token configuration and account-scoped recovery evidence control calls. Cleanup checks retain business/account binding while allowing membership loss, pause, expiry and soft deletion. Recovery keeps the maintenance interval while bounded work remains. Live credentials, verified quotes, provider acceptance and deployed timing/recovery evidence remain unfinished; no activation occurred.

- Research scheduler composition now binds each attributed job to the owning business and actual provider account before submission. Immutable private account routing survives restart and is required for polling, stop delivery and reconciliation; changed accounts and unverified legacy routes fail closed. Two lifecycle/isolation fixtures cover normal recovery, account changes, foreign business identities and denied execution. Agent callback wiring is now present; live deployment configuration and evidence remain incomplete.

- Stopped research now has a separate accounting-only reconciliation component used by the internal scheduler. It verifies bounded terminal pagination and successful-page URLs without importing HTML or claims, deduplicates against earlier evidence, and releases page reservations after final settlement while retaining supplier commitments. Independent tenant/provider gate wiring and live recovery remain incomplete.

- Added an internal bounded research scheduling pass: at most five due jobs, funded/attributed submission selection, persisted fair rotation, polling/stop backoff and lease checks, and separate runner/stopper authorization. Fixture cases verify continued selection after denied jobs across restart, exclusion of unfunded work, stop-before-poll routing and delayed polling. Agent callback/provider composition is now wired; live research stays disabled pending verified deployment prerequisites.

- Owners/managers can now request bounded website research from Business Knowledge. The form sends only URL, 20-page limit and depth two, uses the authenticated idempotency contract, preserves exact in-memory retries after uncertain responses, and opens the validated job. Explicit readiness rejections leave manual knowledge entry available. Two new browser cases cover accepted retries, disabled readiness and accessibility; all 52 browser tests and the frontend production build pass. Live research remains disabled and provider scheduling is incomplete.

- Exhausted research stop delivery now exposes a bounded stop_limit attention reason only while cancellation is pending and no acknowledgement or active lease remains. The app explains paused stop attempts and held reservations separately from status-check failures; settlement clears the warning. The browser fixture verifies list/detail visibility and settlement refresh.

- Internal research stop delivery is separated from execution authorization. The stop-only component requires recorded intent, a known provider job and its own tenant/object/provider gate; it cannot start work or read evidence and retains commitments until verified settlement. Local fixtures cover revoked execution authority, missing intent, rejected stop authority, provider failure and terminal no-op behavior. Stop delivery now persists a 60-second lease, bounded exponential retry delays, an eight-attempt ceiling and a provider acknowledgement receipt. Two further cases verify concurrent rejection, restart deduplication, stale-response fencing and retry exhaustion without releasing commitments. Automated stop scheduling, operator recovery and concrete provider gate wiring remain unfinished.

- Research reservations now retain the original business/requester transactionally. Internal background authorization uses that saved identity and current readiness, membership, role, pause and entitlement; it refuses tenant mismatches and unattributed legacy work. Three new runtime cases verify identity persistence/replay, refusal to adopt unattributed work, and current access checks after pause, role change or membership removal. Full Worker verification passes: 256 tests in 18 files, TypeScript, dry-run bundle and all 503 protected legacy files unchanged. Provider scheduler wiring is present but disabled pending live prerequisites.

- Separate Worker package, local D1 migrations, per-business Cloudflare Agent/Durable Object, private R2 binding and standalone customer PWA. No existing deployment configuration or product code changed.
- Better Auth/D1 Google and Microsoft entrypoints with selected capability scopes, PKCE/nonce, signed identity checks, actual granted scopes, encrypted token custody, refresh preservation, explicit business attachment and local disconnect.
- Tenant directory, current role checks, private conversation records, owner-managed business memory, activity, durable pause and trial/paid text reservations. Paid conversation access now checks the current plan, paid-through/grace/dispute state, release gates and activation evidence before dispatch and before delivery. Responses generated after access expires, a dispute opens or the business is suspended are withheld without charging the customer; provider attempts remain recorded. Included allowances reset on the subscription monthly anniversary, including annual plans; short-month and leap-year boundaries are handled in UTC. New-agent billing retains the first verified subscription usage anchor, and migration 0006 has been applied locally only. Durable provider-attempt limits bound failed and interrupted model calls separately from delivered customer credits. The model path requires explicit configuration; paid execution and external tool execution are not yet enabled.
- Three plan definitions ($349/$549/$899), 28 add-on definitions, 25 meters, 81 automation definitions, ten industry packs, and 18 protected product mappings. These definitions do not mean the executors or subscriptions are live.
- Separate new-agent billing implements setup/activation checkout, portal sessions, period-end cancellation, signed webhook ingestion, event and tenant leases, current-object reconciliation, renewals, payment-failure grace, and refund/dispute facts. Matching paid setup and plan-specific evidence gate activation. These paths pass isolated tests; commerce remains disabled until live evidence and configuration are established.
- Direct Google and Microsoft calendar/mail adapters implement bounded requests, recipient validation, scheduling conflict tokens, provider idempotency where supported, and change-subscription API calls. A server credential broker now renews both providers with encrypted token rotation, serialized refresh leases, compare-and-swap updates, scope reduction and safe reconnect after ambiguous/crashed renewals. The owner/manager calendar-list API uses it and rechecks access/pause before returning normalized choices. Calendar discovery and execution follow bounded server-side pagination, rechecking authority/pause for each page and reporting incomplete or looping directories without exposing cursors. Booking requires a complete directory. Automated writes and synchronization are not yet wired.
- Durable action reviews implement owner-only versioned policies, immutable typed reply/appointment proposals, explicit recipient/resource allowlists, actual scope checks, expiry, pause, current author/requester membership, UTC-day reservations and decision history. Policy changes invalidate old reviews. A gated dispatcher now executes Google/Microsoft replies and appointment creation, verifies paid access and provider readiness, reserves execution claims, checks original recipients/calendar ownership/availability and stores receipts. Exact duplicate effects are suppressed. Pre-dispatch failures require reapproval; dispatched failures and restarts become uncertain and cannot resend. The Approvals UI supports execution/receipts and owner-only appointment-policy creation using verified writable calendar choices, explicit attendee allowlists, daily limits, expiry, and review mode. Uncertain save retries preserve the exact policy ID and payload within the mounted form. Owners can list policies and edit their name, enabled state, recipient allowlist, expiry, preview/approval mode, daily action limit and escalation. Expiry edits are explicitly UTC and invalid recipient lists are rejected before saving. Version conflicts require refresh; disabling remains available while paused and for expired policies. Changes cancel pending approvals, without claiming cancellation of already dispatched effects. The local kill switch remains off, automatic workflows and reconciliation remain unfinished, and no live effect has been performed.
- Website research now has a bounded HTML evidence extractor for metadata, headlines, recognized structured business claims, contacts, public link candidates and offers. Every claim carries source URL, timestamp, selector and extraction confidence and stays unverified/untrusted. It does not execute page scripts, expand remote contexts, fetch links or write approved memory. An internal Cloudflare crawl adapter now bounds submission, result pagination, response size and cancellation requests, validates returned origins and sanitizes malformed provider data. Ambiguous starts never retry automatically. A private per-business research ledger now reserves pages transactionally, prevents request-key reuse with changed inputs, retains uncertain dispatch reservations across restart, handles cancellation/deadline states and settles verified successful-page counts. Private evidence ingestion now binds successful pages to the provider job and approved source origin, deduplicates concurrent deliveries by final URL/content hash, preserves provenance through restart, rechecks settlement after asynchronous extraction and rejects conflicting snapshots or undercounted settlement. Raw HTML is not retained and claims remain unverified. An internal runner now connects submission, cancellation, terminal result ingestion and settlement through durable bounded pagination checkpoints and leased provider polling with persistent backoff/failure/attempt limits, with required authorization callbacks and no automatic resubmission after ambiguity. Late receipts retain known provider identity for cancellation. Provider-reported cumulative browser time is now persisted privately with job identity and observation/terminal metadata; repeated polls do not double-count and missing readings remain unknown. A private supplier commitment ledger now requires a server quote/budget before runner dispatch, atomically claims funding with submission, retains ambiguous commitments, releases only unsubmitted work and counts reconciled overruns against later admissions. Verified quote generation, invoice attribution, account-wide limits and enforceable remote bounds remain unfinished. Authenticated owner/manager research list and evidence reads now enforce current membership and workspace isolation, use bounded pagination/no-store responses and omit provider identifiers, request keys and billing periods. Read access remains available while paused. Server-side allowance derivation now checks operator membership/pause and trial or verified paid activation, maps the catalog crawl-page limits to monthly anniversary periods (including annual plans), and supplies a one-crawl trial cap enforced by the durable ledger. The authenticated research POST now creates idempotent bounded reservations using server-derived allowance/period/deadline values, rejects client-supplied budget fields, and requires the disabled research flag plus current network, permissions, supplier-cost, provider and scheduler evidence. An idempotent persisted 60-second Agent maintenance schedule now handles local job deadlines, recovers on startup, releases only unsubmitted commitments and removes itself when idle. Deployed alarm timing and provider scheduling remain unverified. It does not dispatch a provider crawl. A separate current-operator cancellation API records withdrawal identity/time durably, remains available while paused/expired/disabled, and exposes queued cancellation versus pending/uncertain provider outcomes in the research UI. Live provider activation remains disabled; provider runner scheduling and operator reconciliation, network-safety acceptance and customer-facing research remain unfinished.
- Customer app now includes an owner/manager Website Research panel within Business Knowledge: paginated job/source views, unverified claim labels, source links/timestamps, extraction notes, manual fallback, safe text rendering and stale/error/offline handling. An owner-only confirmation API now copies a reviewed stored claim into business knowledge with a source URL and atomic provenance/activity receipt, rejects changed values and existing topics, and prevents replay from recreating deleted facts. Migration 0007 has been applied locally only. The owner confirmation form now requires explicit review, preserves exact payloads after uncertain responses, refreshes saved knowledge after confirmation, and distinguishes previously removed claims. Managers can inspect sources but cannot confirm. Live research initiation remains unfinished. Customer app implements signup selection, workspace creation/switching, conversation UI, memory, permissions, connections, usage/pricing display, pause, setup/activation checkout, billing portal, period-end cancellation and order history. Billing now displays included text-credit limits, completed and in-progress responses, available credits, and local-time reset dates through an independently refreshed, authenticated no-store usage endpoint. Failed/offline/unverified readings do not display invented zero consumption or stale totals; internal subscription identifiers stay hidden. Checkout buttons honor exact-plan readiness; return URLs never confirm payment. Unimplemented dashboard areas show honest unavailable/empty states. No customer data is fabricated at runtime.
- Git protection check covers 503 existing files, including payment routes/helpers, old migrations, old client, existing Workers, dependencies and deployment configuration. Synthetic agent events pass through the actual old webhook functions with no database/fulfillment effects.

## Evidence and limitations

Latest service verification: **482 tests passed across thirty-one files** in local workerd, covering tenant isolation, signed OAuth fixtures, actual unchanged legacy handlers, catalogs, connector adapters, billing, model-call reservations, durable action reviews and credential renewal. TypeScript, the 503-file protection guard and independent Worker dry-run bundle passed. This includes regressions for different Stripe events arriving concurrently, setup-plan mismatch, refund ownership conflicts, revoked memberships, cross-business agent binding, failed model attempts, concurrent review limits, immutable action hashes, policy revision/expiry, author demotion and Google/Microsoft calendar scopes. Nineteen credential cases cover both providers, rotated/narrowed scopes, concurrent renewal, revocation/new consent during refresh, invalid grants, crash leases, bounded responses and normalized calendar reads withheld after pause/revocation. The signed OAuth fixture traverses policy creation, proposal, approval and history through the actual Worker API and proves wrong-origin decisions fail; it also prevents reuse of a failed refresh token through the identity-account fallback. Fifteen execution cases cover Google/Microsoft reply and appointment receipts, paid-access/readiness rejection, revoked grants, duplicate/concurrent requests, execution-day limits, overlapping bookings, busy calendars, owner pause, fresh approval after pre-dispatch failure, and uncertain outcomes after dispatch or restart. Six directory tests additionally cover per-page access checks, cursor loops, duplicate permissions, request/resource bounds, revocation and malformed authority; provider integration tests cover both cursor formats and booking from a later page. Provider requests in these cases use explicit fixtures; they do not prove live delivery. Nine additional cases verify monthly allowance boundaries, invalid/future anchors, monthly/annual paid chat, exact replay accounting, expired entitlement rejection, and stable usage anchors across later billing-date changes. Six paid-generation recovery cases cover expiry/dispute/suspension during inference, retry accounting across periods, the final shared credit under concurrent requests and the independent provider-attempt ceiling. Seven extraction fixtures cover source provenance, recognized business/offer claims, safe link candidates, untrusted instructions, ignored private claims/remote contexts, malformed structured data, robots warnings and input/output limits. Eighteen crawl-adapter fixtures additionally cover request restrictions, unsafe URL shapes, limits, ambiguous submission, pagination, cross-origin records/redirects, disallowed content, oversized/malformed responses, provider identifiers, cancellation and sanitized invalid URLs/statuses. Six durable-ledger cases additionally verify atomic allowance reservations, immutable replay, restart uncertainty, cancellation settlement, page/deadline limits, period accounting and expiry transitions. Four evidence-storage cases additionally cover concurrent replay, persisted provenance, conflicting content, foreign providers/origins, disallowed pages, page limits, settlement during extraction, redirect deduplication and job-scoped reads. Five runner cases additionally exercise single submission, pagination restart, ambiguous outcomes/late receipts, running snapshots, looping cursors, revoked access during polling and cancellation settlement. Research-read isolation tests additionally cover owner/manager access while paused, denial for viewer/staff/billing/support, revocation, foreign jobs/objects and redaction of provider/billing internals. The signed OAuth HTTP fixture verifies authenticated empty lists, no-store, missing jobs, invalid pagination and foreign/unauthenticated rejection. The research isolation scenario also verifies concurrent owner confirmation, exact reviewed-value checks, existing-topic conflicts, preserved provenance, rejection of manager/other-role confirmation, and retry after memory deletion without recreation. Eight allowance cases additionally cover trial/operator/pause checks, invalid or expired trials, all three paid page allowances, annual/monthly period equivalence, readiness/dispute rejection and the durable single-trial-crawl cap. The reservation integration case verifies disabled/missing gates, rejected budget overrides, trial limits, normalized exact replay, changed-key conflicts, pause, and expired queued-job replacement without reviving the original request. Its eight-test file was rerun after the expiry adjustment. The current full run also verifies SDK maintenance interval deduplication, startup recovery, expiry callback execution and idle interval removal/recreation. The signed OAuth HTTP fixture confirms the disabled POST response. Three provider-compatibility cases cover excluded external/private skipped candidates without breaking pagination, while rejecting unsafe redirects on error responses. All 21 adapter cases and TypeScript were rerun after restricting the exception to skipped records. Three usage-observation cases verify unknown versus explicit zero, restart retention, cumulative rather than additive accounting, stale readings, invalid numbers and provider identity. The pagination runner scenario also verifies one cumulative measurement across repeated terminal result pages. Three supplier-spend cases verify atomic pre-dispatch funding, immutable reservation terms, budget exhaustion, uncertain-charge retention, overrun accounting, exact reconciliation replay and release only before submission. Existing runner cases now reserve fixture spending explicitly. Three polling-work cases cover concurrent claim rejection, expired-lease takeover, stale callback fencing, persistent backoff, repeated-failure review and a finite total attempt bound. Pagination tests advance only the persisted retry time to exercise resumption without wall-clock sleeps. These fixtures do not prove DNS/private-network isolation or live robots enforcement. These prove parser behavior, not live crawling or business verification. Local `/api/health`, `/api/session`, `/api/auth/capabilities` and `/api/catalog` return 200/no-store; unauthenticated `/api/tenants` returns 401.

Frontend TypeScript/production build and **81 Chromium browser tests passed**, including accessibility checks on sign-in, conversation, billing and approvals. Authenticated browser tests use explicit network fixtures. Two research cases cover source rendering, escaped page content, unsafe-link rejection, manual fallback and stale-data removal; the source panel passes accessibility and mobile width checks, with an inspected mobile fixture capture. Both were rerun after the final layout change. Two additional confirmation cases verify explicit owner review, immutable uncertain retries, saved-knowledge refresh and manager restrictions. The confirmation form passes accessibility checks and has an inspected fixture capture. Research summaries and the panel now expose bounded attention reasons for uncertain submissions and exhausted polling, retaining reservations and cancellation access without claiming ongoing progress or automated recovery. A research-attention browser case verifies list/detail labels, the held-reservation explanation and continuing cancellation access. Service polling tests assert each attention reason and clearing after settlement. A cancellation browser case verifies withdrawal while paused, retained reservations and the absence of a false provider-completion claim. Service isolation coverage verifies cancellation after trial expiry with research disabled, durable replay, role restrictions and revocation. Approval tests cover the exact reviewed hash, escaped untrusted content, paused rejection, staff/viewer restrictions, workspace switching and mobile layout. Four appointment-setup cases cover saved policy content, writable-calendar choice, incomplete results, manager restrictions, save retries, mobile layout, pause and workspace isolation; the form passes the accessibility check. Four saved-policy browser cases cover withdrawal while paused, preserving untouched permissions and concurrent-version conflict recovery, permission edits with exact retry payloads, and invalid recipient rejection; the editor passes accessibility checks. A runtime regression proves expired policies can be disabled idempotently while expired enabled policies remain rejected. Three usage cases cover paid totals/reset dates, independent refresh, hidden subscription IDs, stale-data removal, malformed accounting, unverified allowances and offline behavior. The signed OAuth service fixture also checks the new usage route and rejects another business; its eleven-test suite was rerun after the route addition. Execution cases verify the exact approved hash, distinguish provider acceptance from delivery, and suppress resending after an uncertain outcome. Actual unauthenticated desktop/mobile captures use the running local Worker without interception; both providers correctly report unconfigured. The foundation production PWA shell was verified to reload offline with seven public assets cached and no API/auth/query/private data. These prove layout, contract behavior and shell caching, not live OAuth, mobile installation, payments or automation. Fixture screenshots are explicitly distinguished from the actual local page.

No production deployment, provider registration, price publication, purchase, email send, calendar change, phone provisioning, or legacy webhook replay has occurred. Live OAuth approval, actual scopes/accounts, scoped Cloudflare deployment access, Stripe account/price configuration, customer consent, cost telemetry and pilot evidence remain to be established. No authentication bypass exists in production code to substitute for that work.

## Full-scope traceability

| Goal section | Implemented evidence so far | Required work still open |
| --- | --- | --- |
| A Product | Separate business/product/end-customer concepts; new PWA brand | Real customer workflows and public release |
| B Architecture | Separate Worker/app, D1, SDK Agent, private R2 binding | Separate identity/connector/job service boundaries as needed; Queues, Workflows, Vectorize, gateway deployment; advanced executor |
| C Lifecycle | Idempotent workspace records, membership, distinct agent, durable pause | Resumable provisioning workflow, activation/suspension/offboarding, number lifecycle |
| D Combined signup | Google/Microsoft code and signed local fixtures, explicit scope/grant selection | Real consumer/work/admin consent matrix; production approval; recovery/MFA/invitation email delivery/multiple accounts |
| E Website research | Website intake/public URL-shape validation; bounded source-backed HTML/metadata/structured-claim extractor | Safe fetching and robots, comprehensive schema/entity normalization, durable crawls and evidence persistence, editable brief, recommendations, ownership verification |
| F PWA | Responsive app shell, connected initial routes, browser tests | Streaming, uploads, push, real mobile installation, all remaining functional screens and recovery |
| G Roles/authority | Owner team directory, verified-email invitation acceptance, atomic seat limits, membership revocation and revision-bound role changes; current membership checks, private conversations, owner memory controls; durable policies/reviews; gated paid dispatcher, daily action reservations, receipts and approval UI | Resource-level staff assignments, editing policy resources, mail-policy UI, model proposals, full supplier-cost budgets, temporary support UX, live queued revocation |
| H Connectors/secrets | Encrypted tenant-bound grants, browser token endpoints blocked, server renewal broker and calendar-list API; revoked/uncertain/rotated-token tests | Encryption-key rotation, full registry, live large-directory and account-label verification, additional providers, live revoke/reconnect and dispatch integration |
| I Synchronization | Isolated inbox/outbox schema; durable execution claims, exact-effect deduplication and local calendar-overlap reservations | Authenticated subscriptions/watch renewal, history/delta recovery, queues, dead letters, operator/provider reconciliation, recurring occurrence IDs, full scheduling constraints/DST/rescheduling/compensation |
| J Automations | 81 versioned definitions with permissions, modes, receipts, meters and prerequisites | Actual executors for every in-scope template, suppression and recovery suites |
| K Industries | Ten defined packs and tests; three pilot packs selected | Configured real workflows and pilots; regulated eligibility gates |
| L Portfolio | 18 read-only product mappings; existing payment report | Read-only status adapters, verified account claims, internal operating tenant, unresolved satellite interfaces |
| M Fulfillment | New private-storage and outbox boundaries | Resumable generation/QA/artifact/delivery engine; recovery evidence |
| N Plans | Typed immutable prices/allowances, annual-floor tests | Measured economics, verified Stripe prices, paid plan activation |
| O Add-ons | Typed scope/meter/pricing/readiness catalog | Each executor, meter, customer purchasing/cancellation and live release evidence |
| P Economics | Price inputs and margin gates specified | Purchased text-credit packs, measured supplier costs, versioned cost model, supplier quotes, stress/expected-use validation |
| Q Legacy offers | Existing catalog and dependencies protected | Read-only reconciliation of active agreements/prices; optional new website scopes |
| R Billing | Separate checkout/portal/cancellation service; signed events, tenant leases, setup-plan gates, renewal/grace/refund/dispute fixtures, legacy coexistence, gated snapshot audit and private billing activity feed | Complete historical financial reconciliation and repair, add-on purchasing, plan changes, email notice delivery, full live isolated Stripe matrix, price/admin readiness |
| S Channels | Channel requirements and catalog | WhatsApp onboarding, Twilio calls/transfer/SMS, compliant social publishing, live evidence |
| T Operations/data | Authorization, no-store API, minimized errors, encrypted credentials | Full retention/deletion/export, backups/restores, key rotation, incident/monitoring runbooks |
| U Tests | 482 runtime tests, 81 browser tests and static checks | Entire provider/security/recovery/performance matrix, real mobile and live accounts |
| V Milestones | Baseline and initial identity/tenancy/app work | All remaining gates; no milestone is treated as whole-goal completion |
| W Dependencies | Disabled readiness flags expose missing configuration | Actual account/app/provider approvals, costs, ownership and production access evidence |
| X Deliverables | Runnable separate source, local tests, catalog, CI checks, docs | Remaining implementation, operator/customer guides, runbooks, complete evidence index |
| Y Evidence | Explicit fixtures vs live distinctions | Current provider-console/live SKU reconciliation, usage/margin measurements |
| Z Complete | Not achieved | All goal requirements plus 14 consecutive pilot days across at least three consenting businesses |

## Next integration work

1. Complete the remaining billing reconciliation, add-on, plan-change and notice workflows; retain the existing payment boundary and require live evidence before enabling commerce.
2. Extend the now-connected credential/policy/paid-dispatch path with provider reconciliation, operator recovery, workflow occurrence IDs, complete supplier-cost accounting, owner policy editing and model proposals. Verify directory continuation and account labels with live accounts, then implement synchronization and automatic workflows. Keep all live execution disabled until evidence gates are met.
3. Implement evidence-backed website research and business confirmation; add durable provisioning/research jobs.
4. Complete scheduling, automation executors, remaining app screens, communications, fulfillment and all remaining A–Z deliverables.
5. Configure new staging resources and provider test accounts, perform real provider/cost/security tests, then the required pilot and release gates.

External prerequisites are tracked, but they do not block independent implementation work. The goal must remain active until every requirement has direct current-state evidence.

Industry setup answers now persist in the private, revisioned business brief. Questions derive from the selected industry pack, reuse matching business details and move answered questions out of the unresolved list. Only selected-pack fields are accepted; switching packs clears previous answers in the editor. Clinic questions stay limited to approved public information and staff handoff. Owner review, exact retry receipts and existing role restrictions apply. Service coverage checks inherited answers, unsupported fields and switching to clinic scope; the browser fixture checks saving an answer and moving it into saved answers. These are onboarding context, not action permissions or completed automation executors.

Conversation integration now supplies the current reviewed brief revision, selected industry answers and up to three unresolved setup questions to owner/manager chat. Staff chat does not receive the restricted brief. Context is serialized as complete JSON within 3,000 UTF-8 bytes, prioritizing core business fields and marking omitted/shortened values. Conversation instructions ask one relevant setup question at a time and prohibit claims of saved changes or external actions. This supports using confirmed context; automatic extraction of conversational answers into reviewable proposals remains unfinished. No live model quality or prompt-injection guarantee is claimed by these fixtures.

Conversation-context verification: all 258 service tests across 18 files passed, including model-input interception proving the correct reviewed revision/answers, staff exclusion, foreign-tenant exclusion and no brief mutation. Multilingual context remains parseable JSON within its byte allowance. TypeScript and Worker dry-run passed; all 503 protected legacy files remain unchanged.

Owners can move their own conversation message into a reviewable business-brief draft. They choose a field, see the current draft value, edit wording and explicitly copy or replace it, then separately review/save through the existing revisioned endpoint. Assistant messages do not offer this action. Copied wording clears website-source attribution on the affected field. Conversation draft state is memory-only, scoped to the current workspace and cleared when switching workspaces or identities. Overlong answers require shortening instead of silent truncation. This is a manual bridge; automatic model extraction and proposed field mappings remain unfinished. Frontend build and all 52 browser tests passed, with a focused rerun covering draft isolation and accessibility. The 503-file legacy guard also passed.

Conversation access follows the context used to generate each reply. A private Durable Object visibility table records operator-only or shared scope atomically with each new assistant reply. Current membership controls history reads, idempotent replay and subsequent model history; untagged historical assistant replies default to operator-only. Own user messages remain visible. A demotion during inference withholds the private reply, releases the customer text reservation and retains the failed supplier attempt. Membership is checked after pre-dispatch entitlement reads. This enforces current access to new responses and retained server history; it cannot retract content already delivered to a client.

Reply-visibility verification: all 279 runtime tests in 18 files, TypeScript, Worker dry-run and the 503-file legacy guard passed. New cases prove historical/replay/model-context filtering after demotion, retained staff replies, conservative handling of untagged replies, and withholding an in-flight private reply without consuming a delivered text credit. No production migration or deployment was performed.

Owner chat now requests a structured reply with up to three proposed brief edits. The server accepts only existing brief fields (excluding industry-pack changes), exact excerpts of the latest owner message, bounded values and unique fields; invented values and unknown fields are omitted. Source message IDs are assigned by the server. Suggestions persist atomically with the assistant reply in private Durable Object storage, replay without another model call, and are returned only to the current owner. They never mutate the brief. The PWA opens the proposed field/value in the existing review draft with explicit replacement and save. Plain text remains compatible; malformed JSON envelopes fail the turn. Chat uses a 2,000-byte business-context budget to leave room for extraction instructions. Industry-answer mapping, normalized paraphrases and live model quality evidence remain open; exact excerpt validation is not a proof that the selected field or claim is semantically correct.

Suggestion verification: 262 service tests across 18 files and all 53 browser tests passed, as did both builds, TypeScript and the 503-file legacy guard. Tests cover grounded field/value extraction, invented/unknown/duplicate rejection, malformed envelopes, server-bound source IDs, durable read/replay, owner-only suggestion visibility and opening the correctly selected review field without a write. No live inference, deployment or Stripe changes occurred.

Conversation suggestions now include selected-pack industry questions using qualified industryAnswers fields. The server derives the allowlist and stamps the industry pack from the pre-inference brief snapshot; model-provided pack IDs cannot replace it. A concurrent industry change does not relabel the old suggestion. The review panel blocks a suggestion when its pack differs from the current saved or draft pack, and permits explicit copying into the matching industry-answer draft. Manual conversation copying can also target current industry questions. Shared brief fields and industry answers remain separate; copying an explicit industry answer can override an inherited answer visibly before review/save. Semantic extraction quality and live inference remain unverified.

Industry-suggestion verification: all 264 service tests in 18 files and 55 browser tests passed, plus TypeScript, both builds and the 503-file legacy guard. Tests cover allowed-pack fields, server-bound pack identity despite a forged model value, a concurrent pack change during inference, copying into the industry-answer editor and rejecting a stale pack. Existing Stripe code remains unchanged.

Owner personalization now includes the assistant display name. POST /api/tenants/:tenantId/agent-name requires current owner membership, a bounded control-character-free name, the expected current name and an idempotency key. A conditional D1 receipt, rename and audit event commit in one batch; a replay returns its original result without undoing a later rename. Concurrent edits from the same old name allow one winner. The settings form retains uncertain retry payloads, supports conflict refresh and refreshes the workspace/chat after success. Additive migration 0008_agent_name_changes.sql was applied locally only. It does not alter existing Stripe or product schemas.

Name-setting verification: 266 service tests in 18 files, all 56 browser tests, TypeScript and both builds passed. Service tests verify receipt replay after a later rename, atomic conflicting changes, one audit per change, foreign-tenant denial, manager denial and invalid characters. Browser coverage proves immutable uncertain retry, accessibility and the refreshed conversation identity. The 503 protected legacy files remain unchanged. No remote migration, deployment or provider effect occurred.

Connected-calendar setup now resumes larger directories through opaque, rotating continuation handles held in each business Agent. Provider cursors, accumulated calendars and seen-cursor history stay in private Durable Object storage. Handles bind to the requesting user, grant and provider, expire after ten minutes, and use a two-minute work lease to fence concurrent/stale completion. Each request reads at most five pages, rechecking membership/connection/pause before and after reads. Traversals stop after 100 pages, 10,000 resources, one megabyte of calendar JSON, or a cursor loop; these limits never claim a complete directory. At most ten sessions remain open per business, with expired rows purged on access. Duplicate calendar write access only narrows. The picker offers Load remaining calendars and Reload calendar list and keeps selection disabled until completion. Lost continuation responses require a fresh reload rather than silently advancing an old handle. No live Google/Microsoft directory was read.

Calendar-continuation verification: all 271 service tests in 18 files passed. The browser suite passed 56 cases initially; its new continuation case passed on a focused rerun after replacing an unsupported option-disabled matcher with the actual DOM disabled-property assertion (57 cases now verified). Builds, TypeScript and the 503-file legacy guard passed. Service coverage checks multi-batch traversal/reconstruction, cursor secrecy, scope/expiry rejection, permission loss, duplicate authority, concurrent claims, stale completion and loops. Live directory size/performance and provider acceptance remain unverified.

Connected-account labels now come from the provider-confirmed accountEmail in each encrypted grant. The authenticated grants API decrypts only the signed-in user's own rows, verifies the existing user/provider/account/tenant binding, checks workspace connector-management access before and after decryption, and returns only the validated email as metadata. Revoked, corrupt, unavailable-key or unauthorized labels return null; the UI says identity unavailable rather than inferring the platform login email. Assignment, disconnection, account lists and the calendar picker display the label. Tokens, encrypted envelopes and account subject IDs remain excluded from the response.

Account-label verification: the full service run passed 271 of 272 tests; the signed OAuth case exposed an incorrect assertion expecting an already-revoked mailbox label. After correcting it to require redaction, all 12 tests in that suite passed on rerun (272 service cases verified). All 58 browser tests, frontend build, TypeScript, Worker dry-run and the 503-file legacy guard passed. Coverage proves a work mailbox stays distinct from the platform login, invalid/swapped/revoked labels are hidden, expired tenant membership suppresses labels, and two mailbox choices remain distinguishable. No provider credentials are returned and no live provider call or deployment occurred.

Calendar continuations are now tied to the authorization that created them, not only the stable grant ID. Additive migration 0009 adds authorization_revision; renewed grant storage and explicit revocation increment it, while routine token refresh does not. A private authorization stamp covers grant owner, provider account, revision and normalized granted scopes. Calendar reads capture the stamp before acquiring credentials, recheck it around every provider page, and store it with continuation state. Reconnects and scope changes invalidate prior handles; legacy sessions without a stamp require reload. A changed authorization during a read withholds the page. Migration 0009 was applied locally only; no remote state changed.

Authorization-revision verification: all 274 service tests in 18 files, TypeScript, Worker dry-run and the 503-file legacy guard passed. Cases verify a stable stamp after token refresh, a different stamp after renewed consent, rejection after revoke, no returned calendar page after mid-read reauthorization and rejection of continuation under a changed stamp. No frontend code changed in this step; the previously verified 58 browser cases remain unchanged.

New billing snapshot audits: a separate scheduled Worker handler checks up to five due mapped agent subscriptions per invocation. The configured 15-minute trigger remains inert while AGENT_BILLING_RECONCILIATION_ENABLED=false. Enabling also requires account-scoped stripe_account_verified and billing_reconciliation_tests readiness evidence and a matching GET /v1/account result. The audit reads only subscription/latest-invoice objects reached through existing new-platform customer/order mappings; it verifies IDs, metadata domain, price, environment and invoice ownership, records status/cancellation/item/latest-invoice discrepancies, and rejects concurrent local subscription changes. It never changes Stripe objects, invoices or entitlements. Durable per-business leases fence overlapping writes; checked reports are due after 24 hours, failures after one hour. Migration 0010 was applied locally only. This is a scoped discrepancy detector, not complete financial reconciliation: historical invoice/refund/credit/dispute sweeps, missed-event repair, operator review UI, notices and capacity validation remain open. Five checks per 15-minute tick imposes a maximum nominal 480 checks/day before failures; scaling beyond that needs more bounded work capacity.

Billing-audit verification: all 277 service tests in 18 files, TypeScript, Worker dry-run and the 503-file legacy guard passed. New tests prove read-only comparisons, discrepancy detection without entitlement changes, legacy metadata rejection, disabled/readiness gates, daily due suppression, sanitized failure backoff and concurrent-webhook detection. Existing 58 browser tests were not rerun because no frontend changed. No remote deployment, live Stripe read or existing webhook change occurred.

The new billing audit now compares local paid-through access with the latest paid invoice's complete matching-price line periods. It reports missing/behind paid-through dates, unknown payment settlement, currency mismatch, missing owned-price lines and incomplete/invalid periods. An unpaid latest invoice does not invalidate earlier paid access, and a local paid-through date later than the latest invoice is not assumed wrong. These findings remain diagnostic and do not extend, revoke or repair access; credits, disputes and historical invoice evidence still need the separate recovery workflow.

Paid-period verification: all 279 service tests in 18 files, TypeScript, Worker dry-run and the 503-file legacy guard passed. New coverage detects a later paid invoice period without mutating entitlements, rejects incomplete/foreign-price evidence and preserves the distinction between unpaid latest invoices and previously paid access. Billing auditing remains disabled and nothing was deployed.

Billing-report visibility verification: the full service suite passed 280 cases in 18 files, followed by all 25 billing cases after malformed-report and inherited-property-code hardening (281 distinct service cases verified). The full browser suite passed 59 cases, followed by all 13 billing browser cases after adding stale/failed/malformed coverage and strengthening the workspace-switch assertion (60 distinct browser cases verified). Both TypeScript checks, frontend build, Worker dry-run, accessibility check and the 503-file legacy guard passed. Existing checkout and webhook remain untouched. No remote deployment, live Stripe call or commerce activation occurred. The full goal remains in progress; historical financial reconciliation, recovery and live release evidence remain open.

Owner-directory pagination verification: all 291 service tests in 19 files and 68 browser tests passed, alongside TypeScript, frontend build, Worker dry-run and the 503-file legacy protection guard. Coverage reads 106 memberships and 55 invitations with tied timestamps, rejects foreign cursors and revoked owner authority, and verifies independent browser continuation plus clearing on denial. Migration 0015 was applied locally only; nothing was deployed or sent externally.

Recipient pagination verification: all 292 service tests in 19 files and 69 browser tests passed, plus TypeScript, frontend build, Worker dry-run and the 503-file legacy guard. A fixture creates invitations from 52 distinct businesses, accepts the page anchor, reaches the remaining entries and rejects foreign/unverified identities. Browser coverage verifies appended invitations, accessibility and clearing after authorization denial. Migration 0016 applied locally only; no deployment or external messages.

Platform-email foundation verification: all 298 service tests in 20 files passed, plus TypeScript, Worker dry-run and the 503-file legacy guard. Six new fixture cases cover exact fixed-origin requests, retry-window refusal, payload/header boundaries, ambiguous outcomes without internal retry, error classification, owned receipt verification and the invitation template. No frontend changed; the previously passing 69 browser cases were not rerun. No email was sent, no credentials were added and nothing was deployed. Durable dispatch and live delivery evidence remain unfinished.

Invitation outbox state verification: all 301 service tests in 21 files passed, plus TypeScript, Worker dry-run and the 503-file legacy guard. New cases prove frozen preparation, concurrent claim exclusion, fixture transport acceptance, restart recovery with the original key/payload/time, stale-result fencing, retry backoff, conservative rejection after uncertainty, foreign-route/tenant denial, revoked invitation/owner denial and expired retry-window review. Migration 0017 applied locally only. No frontend changed; previously verified 69 browser cases were not rerun. No live email, credentials or deployment.

Email authority-reconciliation verification: all 303 service tests in 21 files passed, plus TypeScript, Worker dry-run and the unchanged 503-file legacy guard. Tests cover never-attempted cancellation, expiry, paused state preservation, immutable pre-dispatch payload/key/time checks, route mismatch, current authority withdrawal and expired-lease review without false cancellation. No frontend changed; the previously verified 69 browser cases were not rerun. No live email or deployment; complete operational sender wiring remains unfinished.

Sender readiness verification: all 306 service tests in 22 files passed, plus TypeScript, Worker dry-run and the 503-file legacy guard. Tests prove disabled-before-database behavior, no legacy-key fallback, exact configuration binding and rejection of incomplete/revoked/empty/expired/future/overlong evidence or unsafe origins. Migration 0018 applied locally only. No frontend changed, so the previously verified 69 browser cases were not rerun. No credentials, real approvals, emails or deployment were introduced.

Verified sender/outbox integration verification: all 309 service tests in 22 files passed, plus TypeScript, Worker dry-run and the 503-file legacy guard. Added fixtures prove credential rotation cannot redirect prepared work, changed senders block dispatch, disabled readiness prevents preparation/claims, and accepted outcomes survive readiness withdrawal. No provider calls or deployment occurred. Browser tests were not rerun for this backend-only change; the previous 69-test browser result is unchanged. The complete implementation objective remains in progress.

Recipient suppression verification: 312 service tests in 22 files passed, plus TypeScript, Worker dry-run and the 503-file legacy guard. New fixtures cover global cancellation without consuming attempts, workspace isolation, newly suppressed active claims, preserved historical acceptance, uncertain retry review and no revival after release. Migration 0019 applied locally only. Browser tests were not rerun for this backend-only change; prior browser evidence remains 69 passing tests. No external mail or deployment occurred. Full goal remains in progress.

Email capacity ledger verification: all 315 service tests in 23 files passed, plus TypeScript, Worker dry-run and the 503-file legacy guard. Added tests exercise concurrent last-slot reservation, exact retry reuse, tenant and period isolation, reduced/expired allowance eligibility, unattempted cancellation release, ambiguous commitments and acceptance consumption. Migration 0020 applied locally only. Browser tests were not rerun for this backend-only change; prior 69-test browser evidence is unchanged. Paid entitlement resolution and dispatcher integration remain open, along with supplier commitments and live delivery. Full goal remains in progress.

Subscription email allowance integration verification: 318 service tests in 23 files passed, plus TypeScript, Worker dry-run and the unchanged 503-file legacy guard. New tests prove all three plan allowances, monthly windows under annual billing, failed trial/revoked activation before attempts, disputed/mismatched/obsolete records, expiry after claiming and durable consumed capacity after historical acceptance. Existing ledger tests cover last-slot concurrency and exhaustion. No deployment, new migration or live provider calls. Browser tests were not rerun for this backend-only change; prior evidence remains 69 passing browser tests. Full goal remains in progress.

Supplier email budget verification: all 321 service tests in 23 files passed, plus TypeScript, Worker dry-run and the 503-file legacy guard. Added tests prove one shared last-slot budget across two tenants, revoked evidence before dispatch, historical acceptance retaining cost, missing routes, exact retry reuse, tenant isolation, increased estimates, expired evidence and release only after unattempted cancellation. Migration 0021 applied locally only. Synthetic fixture amounts are not production prices or approvals. Browser tests were not rerun for this backend-only change; prior 69-test browser evidence stands. No live provider request or deployment occurred. Full goal remains in progress.

Invitation dispatcher verification: 324 service tests in 23 files passed, plus TypeScript, Worker dry-run and the 503-file legacy guard. New fake-transport integration tests exercise concurrent dispatch with one request, accepted capacity settlement, no duplicate send after acceptance, lost-response recovery with unchanged payload/key/first-attempt timestamp and durable backoff, disabled/suppressed no-send behavior and acceptance after in-flight readiness withdrawal. No network email, deployment or migration occurred. Browser tests were not rerun for this backend-only change; prior 69-test browser evidence remains unchanged. Full goal remains in progress.

Delivery receipt verification: 327 service tests in 23 files passed, plus TypeScript, Worker dry-run and the 503-file legacy guard. Then the 18-test email integration file passed again after strengthening coverage for a newly observed hard bounce after operator suppression release. Tests cover sticky delivery facts, non-delivery open/click events, concurrent polling, complaint/bounce suppression, repeated-fact release preservation, payload mismatch, tenant isolation and changed credential routes. Migration 0022 applied locally only; no real receipt request, email or deployment occurred. Browser tests were not rerun for this backend-only change; prior 69-test browser result remains. Full goal remains in progress.

Scheduled email recovery verification: final full verification passed 331 service tests in 23 files, TypeScript, Worker dry-run and the 503-file legacy guard. New tests prove disabled-before-database behavior, healthy work proceeding alongside expired billing, durable blocked-job delay without attempts, receipt checks on later ticks without duplicate send, a five-tenant batch ceiling and corrupt-receipt backoff with zero provider requests. Both email flags remain false. No deployment, migration or real provider requests occurred. Browser tests were not rerun for this backend-only change; prior browser result remains 69 passing tests. Full goal remains in progress.

Owner invitation email flow verification: all 333 service tests in 23 files and all 71 Chromium browser tests passed, plus both TypeScript checks, frontend production build, Worker dry-run and the 503-file legacy guard. New backend tests exercise explicit queueing, one job on repeated requests, sanitized directory state, cross-tenant/role denial and disabled/expired-billing no-queue behavior. Browser tests prove same-invitation retry after an uncertain response, queued-state refresh, acceptance versus delivery copy, disabled controls, unknown-state fallback and accessibility. No migration, deployment or real email occurred; both email flags remain false. Full goal remains in progress.

Platform email usage verification: all 340 service tests in 23 files and 73 Chromium browser tests passed, plus TypeScript, frontend production build, Worker dry-run and the 503-file legacy guard. Tests cover exact warning boundaries, held-to-accepted transitions, tenant separation, owner/billing authorization, unavailable entitlement, response validation, threshold copy, accessibility, denied-read clearing and offline clearing. No migration, deployment, provider send or Stripe checkout/webhook change occurred. Email remains disabled; full goal remains in progress.

Signed email event intake verification: full service verification passed 346 tests in 24 files, TypeScript, Worker dry-run and the 503-file legacy guard. After adding the provider lookup index and strengthening the early-event test, TypeScript and all 37 targeted email tests passed again. Tests cover the independent published Svix vector, multiple signatures, changed body/headers, stale/future timestamps, body size/UTF-8 rejection, duplicate event replay, conflicting signed IDs, capped polling wakeup, no delivery/suppression from hints, and early notification linking after acceptance is later saved. Migration 0023 applied locally only. Browser tests were not rerun for this backend-only change; prior evidence remains 73 passing tests. No real email, webhook enrollment, production replay or deployment occurred. All email flags remain false; full goal remains in progress.

Invitation email cancellation verification: all 348 service tests in 24 files and 74 Chromium browser tests passed, plus both TypeScript checks, frontend build, Worker dry-run and the 503-file legacy guard. Cases cover cancellation while paused/disabled, retained pending invitation, released unattempted tenant/supplier reservations, one audit on retry, tenant/role rejection, blocked dispatch after cancellation, held uncertainty, preserved in-flight acceptance and same-request UI retry/accessibility. Migration 0024 applied locally only. All email flags remain false; no real email or production change occurred. Full goal remains in progress.

Local email maintenance verification: all 351 service tests in 25 files passed, plus TypeScript, Worker dry-run and the 503-file legacy guard. New tests prove active-lease preservation, expired cancellation settlement while paused/disabled, retained ambiguous reservations, interrupted unattempted cancellation release, accepted-capacity repair, idempotent repeat cleanup, a 25-job limit and preservation of healthy paused work. No migration, external request or deployment occurred. Browser tests were not rerun for this backend-only change; prior result remains 74 passing tests. Live delivery is still disabled and the full goal remains in progress.

Mailbox page-integrity verification: all 369 service tests in 26 files passed, plus TypeScript, Worker dry-run bundle and the 503-file legacy guard. Eighteen new fixtures test provider response validation through the actual adapters. No production deployment, mailbox request, migration or Stripe change occurred. Browser tests were not rerun for this backend-only change; the prior result remains 74. The full goal remains in progress.

Durable mailbox ledger verification: 382 service tests in 27 files passed, plus TypeScript, Worker dry-run and the 503-file legacy guard. Thirteen new cases cover restart/page resumption, forced transaction rollback, lease races and expiry, identical/concurrent receipt replay, changed replay rejection, renewed consent, revoked grants, pause and role changes, cross-tenant rejection, cursor loops, bounded rounds, reopen without checkpoint reset, Graph bootstrap and folder restrictions. Migration 0025 applied to the local agent database only. No production deploy, provider request or Stripe change occurred. Browser tests were not rerun for this backend-only component; prior result remains 74. The full goal remains in progress.

Mailbox runner verification: 397 service tests in 28 files passed, plus TypeScript, the Worker dry-run bundle and the 503-file legacy guard. Fifteen new tests exercise the composed Google/Graph adapters, credentials and ledger with injected transports: multi-page restart, deletion references, body/cursor redaction, disabled capabilities and gates, paid-access rejection, consent/billing/pause changes in flight, expired history, malformed pages, stale leases, rate-limit Retry-After and transient-failure backoff. No migration, actual provider call or deployment occurred. Browser tests were not rerun for this backend-only change; prior result remains 74. MAILBOX_SYNC_ENABLED remains false and the full goal remains in progress.

Mailbox polling-bound verification: 401 service tests in 28 files passed, plus TypeScript, Worker dry-run and the 503-file legacy guard. New cases verify twelve abandoned attempts, persisted retry timing, attempt reset only after saved progress, stale defer rejection, concurrent streams at the tenant backlog cap, refusal without cursor advancement, other-tenant independence, capacity restored by processing and explicit recovery on the twelfth transient provider failure. Existing restart tests now verify final-round polling delay. Migration 0026 applied locally only. No actual provider request, production deployment or Stripe change occurred. Browser tests were not rerun for this backend-only change; prior result remains 74. The full goal remains in progress.

Recurring mailbox dispatch verification: 406 service tests in 29 files passed, plus TypeScript, Worker dry-run and the 503-file legacy guard. Five new tests cover disabled-before-database behavior, five-job/one-per-tenant bounds, suspended/revoked/not-due/leased exclusions, unchanged-job delay, preservation of concurrent backoff/progress/claims and real BusinessAgent RPC pause/tenant binding. The first fixture run revealed shared test database candidates; explicit per-case fixture retirement corrected isolation. The bundled runner is now reachable only through internal Agent RPC and the independently disabled cron path. No provider request, migration, production deployment or Stripe mutation occurred. Browser tests were not rerun for this backend-only change; prior result remains 74. The full goal remains in progress.

Bootstrap provenance: migration 0028 records bootstrap/incremental origin on immutable page receipts so historical discovery remains distinguishable after a stream transitions. Older receipts default to unknown. No consumer or automated response is enabled by this marker.

Mailbox bootstrap verification: all 414 service tests in 29 files passed, plus TypeScript, Worker dry-run and the 503-file legacy guard. Eight added cases cover initial profile/list/history ordering, resumable enumeration, initialization replay without reset/provider reads, wrong or malformed profile identity/history, consent replacement during initialization, malformed list pages, empty mailboxes and baseline preservation. Saved page provenance survives Gmail's transition. After additionally tagging Graph initial delta pages as bootstrap, the 40 directly affected runner/ledger tests, TypeScript and Worker dry-run passed again. Migrations 0027 and 0028 applied locally only. No provider calls, customer onboarding activation, production deployment or Stripe changes occurred. Browser tests were not rerun; prior result remains 74. The full goal stays in progress.

Mailbox consumer verification: 421 service tests in 29 files passed, plus TypeScript, Worker dry-run and the 503-file legacy guard. Seven new cases exercise current Gmail state despite an old deletion hint, folder-scoped Outlook 404, preserved provenance, mismatched message IDs, pause during retrieval, concurrent/expired consumer leases, transactional acknowledgement rollback, foreign-tenant rejection and snapshot-budget refusal without acknowledgement. The original Gmail assertion was corrected to compare parsed JSON rather than object-key serialization order. Migration 0029 applied locally only. Provider transports remain fixtures; no real reads, automated messages, scheduled consumer, production deployment or Stripe mutation occurred. Browser tests were not rerun for this backend-only change; prior result remains 74. The full goal stays in progress.

Recurring mailbox processing verification: all 427 service tests in 29 files passed, plus TypeScript, Worker dry-run and the 503-file legacy guard. Six new cases cover all three disabled-before-database gates, five-tenant/five-round fairness, draining an actual 10,000-reference queue while polling requires resync, failure isolation, concurrent backoff preservation, active/retry/review exclusions and actual paused-Agent RPC with bounded unstarted-consumer delay. No migrations, production deploy, real provider calls or Stripe changes occurred. Browser tests were not rerun for this backend-only change; prior result remains 74. All mailbox flags remain false; classification, customer onboarding, watch intake, scale/latency, retention and live evidence remain unfinished. The full goal stays in progress.

Customer Gmail setup/status verification: 431 service tests in 29 files and 76 Chromium browser tests passed, plus both TypeScript checks, frontend production build, Worker dry-run and the 503-file legacy guard. Four new service cases cover activation/flags, private-count redaction, old-consent isolation/revocation, foreign accounts and nonoperator denial. Two browser cases cover scoped setup, saved progress, offline clearing, unknown/invalid status, uncertain setup recovery and accessibility. The new panel's contrast was corrected after Axe reported 4.41:1; the offline fixture now uses actual browser offline mode. No migration, live mailbox read, production deployment or Stripe mutation occurred. All mailbox flags remain false; the full goal stays in progress.

Outlook folder adapter verification: all 444 service tests across 30 files passed, plus TypeScript, Worker dry-run and the 503-file legacy guard. Thirteen new cases cover root/child requests, opaque continuation, encoded IDs, malformed collections, parent consistency, foreign cursor rejection and missing read consent before network access. Provider behavior checked against Microsoft Graph list-mailFolders/list-childFolders documentation. No live provider calls, production deploy, migrations or Stripe changes. Browser tests were not rerun for this backend-only adapter; prior result remains 76. The complete goal remains active.

Resumable Outlook inventory verification: all 451 service tests in 31 files passed, plus TypeScript, Worker dry-run and the 503-file legacy guard. Seven new Durable Object tests cover nested root/child traversal across session reconstruction, private cursors, stale handles, user/account/consent/expiry boundaries, revocation without checkpoint advancement, concurrency/stale completion, duplicate/loop/size/page limits and completed-inventory selection. No production calls, deployment, D1 migration or Stripe changes occurred. The component is currently exercised by tests only; authenticated service/UI integration remains next. Browser tests were not rerun; prior result remains 76. The full goal remains in progress.

Authenticated Outlook discovery verification: 456 service tests in 31 files passed, plus TypeScript, Worker dry-run and the 503-file legacy guard. Five added tests exercise service traversal with Durable Object storage, private cursors/stale handles, disabled and foreign/nonoperator access, mid-read consent and pause changes, and actual Agent RPC tenant binding/disabled behavior. The first full run exposed an existing mixed-clock test: a restarted MailboxSync used wall time while the original fixture used a fixed clock. Both instances now use the same injected fixture clock; the rerun passed. Production scheduling behavior was not changed. Browser tests were not rerun (prior 76); no live reads, deployment, migrations or Stripe mutations occurred. Full goal remains active.

Outlook discovery UI verification: frontend TypeScript/production build and all 78 Chromium tests passed, including new discovery/continuation, nested/hidden labels, offline clearing, malformed/expired results and Axe checks. Axe initially caught a skipped heading level; corrected the panel to h3. Legacy protection still verifies all 503 files unchanged. Backend source was unchanged this turn; previous service result remains 456. Browser transport is fixture-based, with no live mailbox reads or deployment. Folder selection/activation remains pending; full goal active.

Outlook selection/setup verification: 459 service tests in 31 files and 78 Chromium tests passed, plus both builds, TypeScript, accessibility and the 503-file legacy guard. Added service cases verify selected-folder validation, idempotent preservation of existing recovery/cursor state, forced SQL-trigger failure rolling back the entire selection, disabled processing and renewed-consent rejection. The browser test now covers disabled empty selection, exact same-payload retry after lost response, locked pending choices and explicit configuration receipt. No live provider access, production deployment, D1 migration or Stripe change occurred. All release flags remain false and full goal active.

Persistent Outlook status verification: 461 service tests in 31 files and 79 Chromium browser tests passed, along with both builds, TypeScript and the 503-file legacy guard. Two service cases cover multiple-folder initialization/delta completion, pending aggregation, redaction, recovery/paused/disabled state, foreign-account rejection and old-consent exclusion. A browser case verifies saved counts/recovery state, invalid-count rejection and offline clearing; existing Gmail status/setup cases remain green. No migration, live provider call, production deploy or Stripe change occurred. All mailbox flags remain false; full objective active.

Mailbox consent-retirement verification: 464 service tests in 31 files passed, plus TypeScript, Worker dry-run and the 503-file legacy guard. Three cases verify retirement/lease fencing with account isolation and new-consent restart, ordinary token refresh versus actual revocation, and forced retirement failure rolling back the grant revision/ciphertext and all queue changes. Migration 0030 applied successfully to local D1 only. Browser source unchanged; prior browser result remains 79. No live provider calls, production deployment or Stripe changes occurred. Full goal remains active.

Mailbox access-withdrawal maintenance verification: 466 service tests in 31 files passed, plus TypeScript, Worker dry-run and the 503-file legacy guard. Two new cases cover expired membership with a live consumer lease, disabled-provider cleanup, preserving paused queues, repeated cleanup becoming idle, per-stream SQL failure rollback, continuing another offboarding tenant and later recovery. No migrations, provider calls, production deploy or Stripe mutation. Frontend unchanged; prior 79 browser tests remain the latest result. Full goal active.

Historical mailbox consent review verification: 468 service tests in 31 files passed, plus TypeScript, Worker dry-run and the 503-file legacy guard. Two new cases verify retirement of simulated pre-trigger stale consent, preservation of current pending work and live leases, and rejection of a stale grant snapshot with successful later review. Migration 0031 applied locally only. No provider calls, remote deployment or Stripe changes. Frontend unchanged; prior 79 browser tests remain latest. Full goal active.

Account mailbox stop verification: 470 service tests in 31 files and 79 browser tests passed, plus both builds, TypeScript and the 503-file legacy guard. Added service coverage verifies repeated stop, unchanged credential/scopes/revision, preserved pending/cursors, fenced active callbacks, foreign-account denial, scheduler exclusion and maintenance preservation. Browser coverage confirms explicit confirmation, POST body and authoritative stopped-state refresh. Migration 0032 applied locally only. No live provider calls, production deploy or Stripe changes; all execution flags false and full goal active.

Mailbox resume verification: 472 service tests in 31 files and 79 browser tests passed, plus both builds, TypeScript and the 503-file legacy guard. Two new service cases prove repeated resume preserves progress, subsequent stop rejects the old revision, and disabled processing/paused Agent leave monitoring stopped. Browser coverage now confirms the revision payload and continued needs-attention status after resume. Migration 0033 applied locally only. No live provider calls, deployment or Stripe changes. Full objective remains active.

Mailbox restart primitive verification: 475 service tests in 31 files passed, plus TypeScript, Worker dry-run and the 503-file legacy guard. Three new cases cover Gmail reset/cache invalidation and re-observation, stale callbacks, idempotent replay after new progress, changed-key-payload rejection, healthy/stale-round rejection, Graph fresh enumeration and full transaction rollback on forced pending-retirement failure. Migration 0034 applied locally only. No live reads, production deploy or Stripe changes. Frontend unchanged; latest browser result remains 79. Full goal active.

Guarded mailbox restart service verification: 478 service tests in 31 files passed, plus TypeScript, Worker dry-run and the 503-file legacy guard. Three new cases cover Gmail baseline capture and completed retry without more reads, wrong-round replay rejection, account stop during profile capture with no reset receipt, and Graph reset without network baseline access. Migration 0035 applied locally only. No live provider calls, production deploy or Stripe changes. Frontend unchanged; prior browser result remains 79. Full goal active.

Customer mailbox recovery verification: 480 service tests in 31 files and 80 Chromium browser tests passed, plus both builds, TypeScript, Axe coverage and the 503-file legacy guard. Two service cases verify private review counts, offer reuse, exact completed execution replay, healthy-stream exclusion and foreign/expired handles rejected before provider calls. A browser case verifies review before confirmation, uncertainty retry with unchanged offer ID and refreshed initializing status. No D1 migration, live provider call, production deploy or Stripe changes. Full goal active.

Mailbox scan freshness verification: 482 service tests in 31 files and 81 Chromium browser tests passed, plus both builds, TypeScript and the 503-file legacy guard. New cases cover partial versus empty completed scans, preservation through the next partial round, reset clearing, oldest-folder aggregation, incomplete-folder withholding and renewed-consent exclusion. The browser case covers delayed-scan warning, current refresh and invalid timestamp rejection. Migration 0036 applied locally only; no live provider calls, deployment or Stripe changes. Full goal active.
