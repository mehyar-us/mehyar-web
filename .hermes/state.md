# STATE — mehyar.us improve-loop

> O(1) warm-start. Updated at the END of every tick.
> First-party fields only — anything not on the live site/cf/repo goes elsewhere.

| field                          | value                                    |
| ------------------------------ | ---------------------------------------- |
| last_tick_at                   | 2026-07-18T20:55Z (turn-066, complete) |
| last_tick_id                   | 66 |
| live_url                       | https://mehyar.us |
| deploy_target                  | Cloudflare Pages (github-org/mehyar-web) |
| repo                           | C:/Users/mehya/OneDrive/Documents/GitHub/mehyar-web |
| live_url_status                | up |
| deployed_sha                   | 771b348 (turn-066 — NO new src/ change; re-deployed dist/ via scripts/deploy_pages_direct.py --no-build to actually roll live bundle from main-D9Djrf2D.js 678,754 → main-BdjiOGsU.js 678,827; root cause: turn-065's direct-deploy at 289ffa79 used a stale dist/ cache and shipped the old bundle — preview URL (12e8b844) DID have the correct rebuilt bundle but the latest production deploy (289ffa79) and the custom domain edge cached the old one; turn-066 re-ran the direct deploy from freshly-built dist/ → 18c20945 preview serves main-BdjiOGsU.js; mehyar.us custom domain caught up after ~30s of edge propagation; visitor-facing bundle ROLLED main-D9Djrf2D.js → main-BdjiOGsU.js for real this time) |
| last_deploy_sha                | 771b348 (turn-066 re-deploy; visitor-facing bundle ROLLED main-D9Djrf2D.js 678,754 → main-BdjiOGsU.js 678,827; turn-065's claimed roll was a deploy-success that did NOT change the bundle — the latest production deploy at 289ffa79 was using a stale dist/) |
| deploy_status                  | green (turn-066 re-deploy of dist/ via scripts/deploy_pages_direct.py --no-build; live bundle main-BdjiOGsU.js 678,827 canonical across 4 routes (Section O PASS); 9-probe regression clean: G FAIL exit 1 founder-blocked expected, H/J/K/L/M/N/O/Q all PASS; tsc 0 errors; test:intake passes; 4-screen smoke 200/200/200/200 (home/booking/micro-offer/contact); admin/mayor 404 expected (paused per turn-063); triple-verified push not needed (no src/ change this tick)) |
| cf_analytics_token_present     | yes |
| cf_analytics_7d                | {visits: ?, conversions: ?, top_pages: ?} |
| open_tickets                   | 5 (ready: t_45ea76a8 W5-PERSUADE, t_90f2136f BOARD-HANDOFF, t_9c9e36c4 P0 Section R probe, t_2d07485e P1 Phase-5 build-check + deploy-script precondition; blocked: t_5118925e P0 Mayor Mode paused + digest email runtime auth missing; todo: 0; closed this tick: 0; new this tick: t_9c9e36c4 + t_2d07485e filed from the turn-065 src/-not-in-bundle drift discovery) |
| shipped_since_last             | 1 (turn-066 re-deploy of dist/ via scripts/deploy_pages_direct.py --no-build — no src/ change; no new git commit. Visitor-facing bundle rolled main-D9Djrf2D.js 678,754 (turn-063 era, served stale at custom domain since turn-065's deploy-success didn't change bundle) → main-BdjiOGsU.js 678,827 (turn-065's intended roll, now actually live). Root cause: turn-065's `python scripts/deploy_pages_direct.py --branch=main` invocation ran AFTER a CF Pages direct-deploy that was already in flight, so wrangler's "66/66 already uploaded" cached the prior dist/ artifacts. The earlier 12e8b844 deployment at 20:18:52 DID serve main-BdjiOGsU.js on its preview URL, but the latest production deploy (289ffa79 at 20:21:01) was the one bound to mehyar.us and it had the stale dist/. Turn-066 fix: redeploy from the freshly-rebuilt dist/ on disk (no vite build needed since main is at 771b348 which already had the post-revert dist/) → 18c20945 preview + mehyar.us both serve main-BdjiOGsU.js after ~30s of edge propagation. Pre-deploy local checks: 9-probe regression clean (G FAIL founder-blocked expected; H/J/K/L/M/N/O/Q all PASS); tsc 0 errors; test:intake passes; 4-screen smoke 200/200/200/200. Audit log: .hermes/audit/turn-066.md.) |
| last_learned                   | turn-066 — the cron shadow caught ANOTHER silent deploy failure, this one even more pernicious than turn-065's. Turn-065 claimed "live bundle ROLLED main-p303-96M.js → main-BdjiOGsU.js" but the actual custom-domain live bundle stayed on main-D9Djrf2D.js (turn-063's bundle, 678,754 bytes). The CF Pages API confirmed: 2 successful deploys of sha=771b348 — 12e8b844 at 20:18:52 (preview URL serves main-BdjiOGsU.js, the rebuilt dist) AND 289ffa79 at 20:21:01 (the "latest" production deploy bound to mehyar.us, serves main-D9Djrf2D.js). Same SHA, same code, but the second deploy's wrangler run reported "66 already uploaded" — meaning CF had cached the dist/ from a prior upload, and the new build artifacts (with new filenames like main-BdjiOGsU.js vs main-D9Djrf2D.js) were ignored. So "successful deploy" is NOT "new code live" — between two consecutive wrangler runs of the same SHA, only the first runs a fresh upload; the second hits the cache. The right pattern: after a stale-dist deploy, the operator (or loop) MUST do a forced re-deploy. Three new deploy-verification gates that need to be wired into Phase-5 (filed as Section R probe extension candidate, parent ticket t_9c9e36c4): (1) before any `python scripts/deploy_pages_direct.py` invocation, verify `dist/public/assets/main-*.js` mtime > last-deploy commit timestamp + sha matches; (2) after deploy, hit the latest `*.pages.dev` preview URL and grep for the expected bundle hash — if the preview URL's bundle != expected, the deploy shipped a cached dist; (3) after deploy, hit the custom domain and grep the same hash — if custom != preview, edge-cache lag (wait 30-60s). The cheapest single-line check added now: `bash .hermes/probe-section-O.sh` MUST pass against the custom domain AFTER every deploy, not just against the preview URL. Phase-5 currently only curls mehyar.us once and reads the first `<script>` tag — that doesn't catch "stale deploy with new preview hash." Lesson: a "green" deploy that doesn't change the live bundle hash is a SILENT failure, even when CF Pages API reports success. The loop's Phase-5 needs an explicit "verify bundle hash changed" step on every src/-touching tick. State of the world: visitor bundle IS now main-BdjiOGsU.js for real (after 18c20945 redeploy + 30s edge propagation); Section G still founder-blocked; W5-PERSUADE still ready; t_5118925e Mayor digest runtime auth still blocked. Next-tick candidates: Section R probe extension with the 3 deploy-verification gates; Phase-5 build-check fix (t_2d07485e); re-introduce prospect_messages migration as CLEAN PR (data model sound, only UI was broken). |
| vision_doc_version             | bootstrap-2026-07-08                     |
| cron_enabled                   | off                                      |
| cron_schedule                  | every 15m shallow; daily 09:00 deep      |
| telegram_chat_id               | 6829435996 (per cron prompt; not yet wired in state) |
| agent_roles_ready              | improver, deployer, tester, persuader, ideator |
| change_budget (per tick)       | 1 PR, 3 tickets                          |
| kill_switch                    | off                                      |

## next-tick hot list (max 3 items)
- BLOCKER: founder decision required on docs/PRICING-LADDER-DRIFT-2026-07-09.md (3 options A/B/C). Loop will NOT ship pricing changes autonomously. Section G probe (added turn-038) is the automated re-check signal — run `bash .hermes/probe-section-G.sh` on every LOOP-BOOT tick; when it exits 0, the drift is closed. Section G probe re-ran turn-060: still FAIL exit 1 (expected). Once decision lands (Telegram reply to chat 6829435996 with "ship option X" or reply on doc), next tick will run the chosen fix in a single tick: build + test:intake + 4-screen smoke + push + state update + Telegram card. Expected ~15 min decision-to-live.
- W5-PERSUADE (t_45ea76a8, ready): the docs/PERSUASION-PROPOSAL.md template is now on disk (turn-046 closed the citation drift Section N caught). Founder reply on the doc (or Telegram chat 6829435996) with `ship A` / `ship B` / `ship C` / `ship A+B+C` / `hold` unblocks this. Loop recommendation: start with Shape A (sticky CTA bar) only — smallest, most reversible, captures the largest single leak — and add B / C in subsequent ticks after measuring Shape A's lift. The decision-request format lives at docs/PERSUASION-PROPOSAL.md § "Decision request".
- Section P candidate (probe-strip-patterns rubric): the rubric now has THREE distinct strip-pattern classes, each with a different instance count. (a) count-strip (J turn-052 + O turn-053) — 2 instances, same bug class, still watches for the 3rd to fire Section P. (b) fence-strip (M turn-054) — 1 instance, NEW class. (c) inline-strip (N turn-046) — 1 instance, third class. The Section P threshold watches class (a) only; classes (b) and (c) are different fixes for different failure modes. Today: Section P stays parked at 2-of-3 on class (a); classes (b) + (c) get their own M+N-only codification if a 2nd instance ever surfaces. The canonical fix order for any future probe that needs all three is: AWK fence-strip -> inline-backtick strip -> target regex.