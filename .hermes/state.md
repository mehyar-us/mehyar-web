# STATE — mehyar.us improve-loop

> O(1) warm-start. Updated at the END of every tick.
> First-party fields only — anything not on the live site/cf/repo goes elsewhere.

| field                          | value                                    |
| ------------------------------ | ---------------------------------------- |
| last_tick_at                   | 2026-07-18T21:50Z (turn-067, complete) |
| last_tick_id                   | 67 |
| live_url                       | https://mehyar.us |
| deploy_target                  | Cloudflare Pages (github-org/mehyar-web) |
| repo                           | C:/Users/mehya/OneDrive/Documents/GitHub/mehyar-web |
| live_url_status                | up |
| deployed_sha                   | eaef3d8 (turn-067 — /apps ship-the-WIP + sitemap.xml + Sitemap.tsx + copy-route-shells.mjs; visitor-facing bundle ROLLED main-BdjiOGsU.js 678,827 (turn-063 era) → main-BN6RneUm.js 678,904 (+77 bytes, vite rebuilt from eaef3d8 src/, then `scripts/deploy_pages_direct.py --branch=main` rolled preview URL f94e24c6 → custom domain caught up after ~45s edge propagation)) |
| last_deploy_sha                | eaef3d8 (turn-067 /apps ship-the-WIP; preview URL f94e24c6 first served main-Bxr873aI.js 678,970 bytes local build, custom domain mehyar.us caught up to main-BN6RneUm.js 678,904 after edge settle) |
| deploy_status                  | green (turn-067 /apps ship-the-WIP; live bundle main-BN6RneUm.js 678,904 canonical across 4 routes (Section O PASS); 9-probe regression clean: G FAIL exit 1 founder-blocked expected, H/J/K/L/M/N/O/Q all PASS; tsc 0 errors; test:intake passes; 4-screen smoke 200/308/308/308 (home/booking/micro-offer/contact); Section O re-ran on custom domain not just preview URL per turn-066 two-assertion rule; /apps live with full title + canonical + Sitemap page row + sitemap.xml entry) |
| cf_analytics_token_present     | yes |
| cf_analytics_7d                | {visits: ?, conversions: ?, top_pages: ?} |
| open_tickets                   | 5 (ready: t_45ea76a8 W5-PERSUADE, t_90f2136f BOARD-HANDOFF, t_9c9e36c4 P0 Section R probe, t_2d07485e P1 Phase-5 build-check + deploy-script precondition; blocked: t_5118925e P0 Mayor Mode paused + digest email runtime auth missing; todo: 0; closed this tick: t_1b8d1793 (turn-067 /apps ship-the-WIP — done); new this tick: t_1b8d1793 filed done capturing the /apps ship-the-WIP — audit log .hermes/audit/turn-067.md) |
| shipped_since_last             | 1 (turn-067 /apps ship-the-WIP — git commit eaef3d8 + vite rebuild → dist/public/assets/main-Bxr873aI.js 678,970 bytes local + `python scripts/deploy_pages_direct.py --branch=main` → preview URL f94e24c6 served main-Bxr873aI.js (local build), custom domain mehyar.us caught up to main-BN6RneUm.js 678,904 after ~45s edge propagation. 3 files changed: client/public/sitemap.xml (+1 line `<url><loc>https://mehyar.us/apps</loc></url>`), client/src/pages/Sitemap.tsx (+2 / -1 — "Apps" row inserted between Founder Story and Blog), scripts/copy-route-shells.mjs (+12 / -7 — /apps added to BOTH routeMeta for per-route title + description + canonical AND directRoutes for build-time static-shell copy). Closes SEO-discoverability gap the /apps route (Rizza + AiMech, commit 889222b) had since turn-067-prep: visitors reached /apps via navbar but a first-time crawler hit sitemap.xml with 8 routes (no /apps), visitors on /sitemap saw a 7-row list (no Apps row), and /apps had no static shell (no per-route JSON-LD, no unique title, no canonical — only SPA fallback). Audit log: .hermes/audit/turn-067.md.) |
| last_learned                   | turn-067 — /apps ship-the-WIP closed the SEO-discoverability gap the /apps route (Rizza + AiMech, commit 889222b) had since turn-067-prep. Three-place coverage rule for any new public route: client/public/sitemap.xml (crawler discovery) + client/src/pages/Sitemap.tsx (in-app Sitemap page row) + scripts/copy-route-shells.mjs (BOTH routeMeta for meta AND directRoutes for build-time static-shell copy). Without the directRoutes entry, dist/public/apps/index.html wouldn't exist and crawlers would get only the SPA fallback (no per-route JSON-LD, no unique title, no canonical). Ship-the-WIP pattern is now 7-data-point (turn-045/050/051/052/056/064/067) — found 3 modified untracked files at tick start, probed Section K (drift closed), committed eaef3d8, deployed via scripts/deploy_pages_direct.py --branch=main, audit in same commit. Pre-deploy local: tsc 0 errors; test:intake green; vite build green → main-Bxr873aI.js 678,970 bytes local. Deploy verification per turn-066 two-assertion rule: preview URL f94e24c6 confirmed main-Bxr873aI.js, custom domain mehyar.us caught up to main-BN6RneUm.js 678,904 after ~45s edge propagation. 9-probe regression clean (G FAIL founder-blocked expected; H/J/K/L/M/N/O/Q all PASS). Triple-verified push per git-credential-helper-hermes skill pitfall #10: GIT_CONFIG_SYSTEM=/dev/null GIT_TERMINAL_PROMPT=0 git push origin main → 6b994f7..eaef3d8 main -> main. Companion class: turn-058/059 closed the home `$330` drift on visitor-facing copy + crawler-indexed FAQPage JSON-LD; turn-060 closed the canonical-target drift on a different route's static-shell vs SPA-SeoManager split; turn-067 closes the static-shell sitemap + Sitemap.tsx page + copy-route-shells drift on /apps. Four lessons reinforced: (1) every new public route needs three places wired or it's invisible to crawlers; (2) ship-the-WIP is now 7-data-point — probe Section K first, commit if sound, audit in same commit, advance; cost of starting fresh is loss of mid-flight audit trail; (3) deploy verification needs TWO assertions per turn-066 rule (preview + custom domain); (4) static-shell meta + per-route JSON-LD is the crawler-readable layer, SPA SeoManager is the post-hydration layer — both must agree or crawlers see a half-route. State of world post-turn-067: visitor bundle main-BN6RneUm.js 678,904 green across all 9 rubric sections; /apps fully discoverable via sitemap.xml + Sitemap.tsx + copy-route-shells.mjs; Section G pricing drift still founder-blocked; W5-PERSUADE still ready; t_5118925e Mayor Mode still blocked on digest email auth; cron still off. Next-tick candidates: Section R probe (sed-placeholder + WIP-marker detection — t_9c9e36c4 P0); Phase-5 build-check precondition (t_2d07485e P1); re-introduce prospect_messages migration as CLEAN PR. |
| vision_doc_version             | bootstrap-2026-07-08                     |
| cron_enabled                   | off                                      |
| cron_schedule                  | every 15m shallow; daily 09:00 deep      |
| telegram_chat_id               | 6829435996 (per cron prompt; not yet wired in state) |
| agent_roles_ready              | improver, deployer, tester, persuader, ideator |
| change_budget (per tick)       | 1 PR, 3 tickets                          |
| kill_switch                    | off                                      |

## next-tick hot list (max 3 items)
- BLOCKER: founder decision required on docs/PRICING-LADDER-DRIFT-2026-07-09.md (3 options A/B/C). Loop will NOT ship pricing changes autonomously. Section G probe (added turn-038) is the automated re-check signal — run `bash .hermes/probe-section-G.sh` on every LOOP-BOOT tick; when it exits 0, the drift is closed. Section G probe re-ran turn-060: still FAIL exit 1 (expected). Once decision lands (Telegram reply to chat 6829435996 with "ship option X" or reply on doc), next tick will run the chosen fix in a single tick: build + test:intake + 4-screen smoke + push + state update + Telegram card. Expected ~15 min decision-to-live.
- W5-PERSUADE (t_45ea76a8, ready): the docs/PERSUASION-PROPOSAL.md template is now on disk (turn-046 closed the citation drift Section N caught). Founder reply on the doc (or Telegram chat 6829435996) with `ship A` / `ship B` / `ship C` / `ship A+B+C` / `hold` unblocks this. Loop recommendation: start with Shape A (sticky CTA bar) only — smallest, most reversible, captures the largest single leak — and add B / C in subsequent ticks after measuring Shape A's lift. The decision-request format lives at docs/PERSUASION-PROPOSAL.md § "Decision request".
- Section P candidate (probe-strip-patterns rubric): the rubric now has THREE distinct strip-pattern classes, each with a different instance count. (a) count-strip (J turn-052 + O turn-053) — 2 instances, same bug class, still watches for the 3rd to fire Section P. (b) fence-strip (M turn-054) — 1 instance, NEW class. (c) inline-strip (N turn-046) — 1 instance, third class. The Section P threshold watches class (a) only; classes (b) and (c) are different fixes for different failure modes. Today: Section P stays parked at 2-of-3 on class (a); classes (b) + (c) get their own M+N-only codification if a 2nd instance ever surfaces. The canonical fix order for any future probe that needs all three is: AWK fence-strip -> inline-backtick strip -> target regex.