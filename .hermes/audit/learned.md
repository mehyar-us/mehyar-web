2026-07-08 · shipped PWA manifest + app-shell SW (turn-001, sha b413ee6) — install prompt + offline shell live; conversion paths excluded from cache.
2026-07-09 · shipped 1200x630 OG PNG + FAQPage JSON-LD + priceRange schema (turn-002, sha b6486c2) — social previews now real images not blank/SVG; FAQPage eligible for SERP rich result. Pure additive. Next: Lighthouse baseline + per-page meta overrides (currently all routes share home meta; conversion pages need their own).
2026-07-09 · shipped per-route meta on pre-rendered shells (turn-003, sha 406eba0) — 13 high-value public routes (/services /about /contact /booking /micro-offer /blog /portfolio /newsletter /330 /free-checklist /privacy-policy /terms /sitemap) now ship unique title/description/canonical/og/twitter instead of all serving the home meta. Closed W4-SEO (t_d0e60303). Also wired scripts/** into CF Pages deploy workflow path filter. Next: W2-FUNNEL move hero CTA → /micro-offer.
2026-07-09 · shipped hero CTA move /contact → /micro-offer#intake (turn-004, sha 22dd6f0) — hero "Book a Tech Audit" copy was an audit-intent signal routed through the slowest funnel (general /contact form). Now lands directly on the dedicated $330 audit page with the intake form in view (scrollY=2190 on #intake anchor). One-line href change in client/src/components/hero-section.tsx, zero risk surface. W2-FUNNEL hero-CTA piece closed (t_d02d9660); deeper funnel smoke t_0634816e still queued. Next: t_0634816e end-to-end smoke.
2026-07-09 · shipped per-offer CTA labels + service-tagged /contact routing on pricing cards (turn-005, sha c8d2507) — six pricing cards had identical 'Book a Tech Audit' copy + /contact href, so a visitor reading e.g. AI Follow-Up ($1,500-$5,000) was mis-routed into the slowest funnel for an offer that did not match. Now each card has offer-specific CTA copy + /contact?service=<slug> so ContactSection mounts mode=booking_call with the service slug in serviceCategory. Card 0 (Audit) still routes to /micro-offer#intake (no regression). CF Pages bundle hash moved main-DPwQXa_h.js → main-Bq9wUcP7.js; bundle grep confirms all 5 new labels + all 5 service= slugs live. npm test:intake green; /contact /booking /404 unchanged 200. Voice score 5/5 (1st-person imperative, concrete verbs, no Learn More, no em-dash, no agency theater). Closed W2-FUNNEL piece 2/2. Next: t_0634816e end-to-end smoke (Booking + MicroOffer submit → CF Functions audit row → conversion tracking), then W1-SLOP over non-pricing public pages.
2026-07-09 · shipped 5 page-level audit CTAs → /micro-offer#intake (turn-006, sha f4a60cd) — About×2, Blog sidebar, Services, 404 all moved off the slowest funnel for audit-intent visitors. Hero (turn-004) + pricing cards (turn-005) + these 5 page-level CTAs (turn-006) = full audit-intent funnel realignment; W2-FUNNEL closed piece 3/3. Live bundle main-ChI82pEY.js confirmed: /micro-offer#intake × 2, /contact?service= × 1, all 5 service slugs preserved. Tick was reconciliation-only: turn-006 was merged locally + deployed live before its journal entry was written, so this tick (turn-007) only pays down bookkeeping. No code changed. Next: t_0634816e end-to-end smoke (full submit → CF Functions → conversion tracking) OR start W1-SLOP over the 4 pages touched in turn-006 (About/Blog/Services/404) since their copy is already structurally clean from the move and only needs voice score 4-5.

