# Learned — mehyar.us improve-loop

> One-line per tick, append-only. The loop's memory. Longer notes go to `.hermes/audit/turn-NNN.md`.

2026-07-09T16:25Z — when the funnel is "3-piece closed" by name, the last 1-2 pieces are usually the most-tempting CTAs (the universal `CTASection` reused across pages, the per-blog/portfolio QuickAnswer). Grep for `href="/contact"` after every funnel realignment to catch the stragglers — they're the highest-leverage targets and the easiest to miss because they look like normal page chrome.

2026-07-09T22:55Z — the founder's stated pricing ladder (VISION.md) and the revenue reality (micro-offer charge) can drift apart silently. The cheapest catch is a 3-line check added to the LOOP-BOOT rubric: for each priced tier card, verify the target intake page charges the same amount. Today's drift: tier-1 ($150 Free Tech Audit) + tier-2 ($250 Website Diagnosis Report) both point at /micro-offer which charges $330 — visible on the live home page since launch, missed by turn-005, turn-028, turn-031, turn-034. Surface drift via docs-only tick when the fix is too consequential for autonomous; don't ship pricing changes unilaterally.
2026-07-10 · turn-041 — working-tree hygiene (sha bf1a21b on github main; docs-only — no CF Pages deploy, live bundle main-BKU1Uoxy.js unchanged). `.gitignore` grew 24 lines (root-level *{_live.html, mehyar_*.html, mehyar_*.xml, p_[N].html} probe snapshots + `.hermes/.probe-section-*-bundle.js` cache + `.hermes/audit/*` noise — with `!.hermes/audit/*.md` exception for the legitimate structured records). 24 untracked root strays removed + 2 cached probe bundles. 4 missing audit `.md` files added (turn-018, turn-028, turn-034, turn-039 — all 4 real loop audit-trail records already referenced from VISION.md diary lines; they existed on disk but never landed in git, a 22-tick drift closed). Reusable `.hermes/patch_home_jsonld.py` added (turn-030's WebPage + BreadcrumbList home-entry patch — CRLF + 2-space-indent + idempotent). Working tree 69 → 6 untracked (the remaining 6 are the 4 newly-added audit .md + the patch script + `.hermes/probe/` dir, left for a future tick). Section G pricing-consistency probe still FAIL exit 1 (tier-1=$150 intake=$330 — founder-decision-blocked, expected); Section H a11y probe 9/9 PASS; Section J build-integrity probe 9/9 PASS; tsc green; test:intake 11/11; 4-screen Phase-6 200/308/308/404. Voice 5/5 unchanged; anti-slop 0 hits on local + live. Lesson: two distinct hygiene drifts accumulated silently over 40 ticks — (1) probe snapshots in repo root, fixable with gitignore; (2) audit `.md` files that exist on disk but never added to git, only catchable by comparing `ls .hermes/audit/turn-*.md` to `git ls-files .hermes/audit/turn-*.md`. The cheap recurring check is a 2-line diff loop — worth adding as `probe-section-K.sh` (turn-040's named "Section K" extension candidate) so audit-record-tracking drift catches itself next audit, ~5s wall time.

2026-07-10 · turn-042 — working-tree hygiene + rubric extension (sha 781d267 on github main; docs-only — no CF Pages deploy). Section K probe-section-K.sh new (.hermes/probe-section-K.sh +114) — bidirectional find + git ls-files + sort + comm -23/-13 diff that catches .hermes/audit/turn-*.md drift in both directions (on-disk-but-not-in-git = ORPHAN P0; in-git-but-not-on-disk = STALE P1). Negative-tested: `touch turn-999-test-orphan.md` bumps on-disk to 29, probe exits 1 with the orphan line printed, after `rm` returns to 28/28 exit 0 — bidirectional drift detection verified. docs/QA-MEHYARSOFT-B2B-BASELINE-2026-05-11.md grew from 10 sections (A-J) to 11 sections (A-K); Sections I and J unchanged (preserves "I = Open registry" + "J = Build-artifact-integrity" mnemonics); new Section K covers .hermes/audit/ drift + the failure-mode catalog (5 rows) + the why-K-not-renumber rationale (same approach turn-040 used adding J). Bonus: 4 reusable probe artifacts added to git (.hermes/probe/turn-017-body.txt + turn-019-fix.py + turn-020-apply.py + extract-services.py) — transient home.html curl snapshot from turn-020 stays gitignored. Section G pricing-consistency probe still FAIL exit 1 (tier-1=$150 intake=$330 — founder-decision-blocked, expected); Section H a11y probe 9/9 PASS; Section J build-integrity probe 9/9 PASS; tsc green; test:intake 11/11; 4-screen Phase-6 200/308/308/404; voice 5/5 unchanged; anti-slop 0 hits on local + live. Lesson: a rubric extension that costs ~5s wall time and watches the LOOP ITSELF (not just the live site) is a textbook cheap-and-right move — Sections G/H/J pin the bundle to src/; Section K pins the audit trail to git. The negative-test pattern is the second-order lesson: any new probe that claims to catch drift must be verified with a synthetic-drift injection (touch an orphan, confirm exit 1, remove the orphan, confirm exit 0) — without that round-trip the probe could pass-on-vacuum and silently never catch anything. Next rubric extension candidate (L): open-ticket-id correctness — assert every ticket-id cited in recent state.md or audit record actually exists in `hermes kanban list`, catches the "stale ticket id reference" drift class that Section K's cousin would have caught had turn-028's "t_5f79e5ac" ever drifted.

2026-07-10 · turn-043 — Section L Open-ticket-id-reference probe (sha 5b1809f on github main; docs-only — no CF Pages deploy). Section L probe-section-L.sh new (.hermes/probe-section-L.sh +141) — grep + sort -u + python sqlite3 (NOT the sqlite3 CLI — Android platform-tools binary returns "unable to open database file" on real Windows paths) that snapshots every t_<8 hex chars> cited in state.md / VISION.md / QA baseline / learned.md / audit/*.md, then reads the mehyar-us kanban DB (47 ticket-ids) and comm -23 cited in-db to surface stale citations. One-way diff: cited \ in-db = STALE (FAIL exit 1); reverse is informational only (open tickets not cited is normal). Negative-tested: appending `t_ffffffff` to state.md bumps cited to 29, probe exits 1 with the line printed, after restoring state.md returns to 28 cited / 47 in DB PASS exit 0. docs/QA-MEHYARSOFT-B2B-BASELINE-2026-05-11.md grew 11→12 sections (A-L); Sections I/J/K all unchanged (preserves the I/J/K mnemonics turn-040/042 locked in); new Section L covers ticket-id-reference drift + 5-row failure-mode catalog (archived ticket cited, id typo, cross-board bleed, DB missing, probe itself untracked) + 4 implementation gotchas (python sqlite3 not CLI, MSYS path translation x2, tempfiles under .hermes/ not /tmp, trap cleanup on EXIT). Section G pricing-consistency probe still FAIL exit 1 (tier-1=$150 intake=$330 — founder-decision-blocked, expected); Section H a11y probe 9/9 PASS; Section J build-integrity probe 9/9 PASS; Section K audit-trail probe 28/28 PASS; new Section L ticket-id probe 28 cited / 47 in DB PASS exit 0; tsc green; test:intake 11/11; 4-screen Phase-6 200/308/308/404; voice 5/5 unchanged; anti-slop 0 hits on local + live. Lesson: identifier-reference drift (Section L) is a different class from file-level drift (Sections G/H/J/K). The two together cover most "user re-verifies on receipt, finds the citation is a lie" failure modes. The cheapest implementation is python's stdlib sqlite3 against the mehyar-us DB — no extra deps, no CLI required (the Windows sqlite3 CLI is the Android platform-tools binary which doesn't work here). The "in-db but never cited is informational, not failure" asymmetry is the right call: open tickets not appearing in state.md is normal (the loop cites ~5 ids consistently, the rest are routine workflow); only the reverse direction (cited but missing) is a fabrication-class bug. Next rubric extension candidate (M): commit-SHA reference correctness — assert every commit-sha cited in state.md / VISION.md / audit actually exists in `git log`, catches the cousin class of stale SHA references (turn-039 and turn-042 both backfilled SHAs across multiple files; future ticks could cite a sha that's been force-pushed away). Cheap (~5s wall time), same python stdlib pattern.
