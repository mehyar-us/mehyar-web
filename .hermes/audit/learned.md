# learned.md — mehyar.us improve-loop (rolling 1-line notes)

Each tick appends ONE line. The audit/turn-NNN.md file is the long-form
counterpart; this file is the O(1) warm-start index for "what does the loop
currently know?"

2026-07-10 · turn-050 — ship-the-WIP pattern: working tree often accumulates real value from interrupted/parallel ticks; cheap check `git status --porcelain | head -20` surfaces shippable WIP for a no-judgment, zero-risk tick. Caught and shipped 4 PWA icon files + sw.js cache bump that had been sitting uncommitted. Lesson is also a closure note: the WIP pile-up is a real recurring failure mode worth a dedicated sweep every ~10 ticks.

2026-07-10 · turn-051 — ship-the-WIP self-applies (caught 1 sweep earlier than the turn-060-ish schedule the turn-050 lesson named). Closed 4-file turn-050 WIP + deleted 9 transient probe-WIP files + .gitignore +6 patterns so the .hermes/<...>-tNNN-*.{html,js,py} drift class won't recur. Section K probe FAIL->PASS delta (36/36 audit files). Three secondary lessons: (a) probe-M intermediate cache files leak the SHAs the probe is inspecting into filenames — a `find .hermes -name '.probe-*-cited.*' | wc -l` probe-O (~2s) would catch this automatically; (b) `learned.md` rolling 1-line format needs `git log --all --oneline -- .hermes/audit/learned.md` as the out-of-band history pointer, not `git log -50`; (c) merge abort with "dirty working tree" + clean `git status` = CRLF-line-ending pre-check confusion, retry once.

2026-07-10 · turn-052 — Section O Live-bundle-URL auto-discovery probe shipped (closes 'probe silently validates stale bundle' drift that had been open 4-5 ticks since turn-027 bundle-roll). H/J refactored to source same discover_live_bundle_url() helper; bonus CRLF bug fix on J probe's grep -c integer compare (latent from turn-040, never fired in prod). Rubric grew 14→15 sections A-O (mnemonic: O = origin URL). Five lessons: (1) a rubric probe that pins its own input is a self-referential invariant — source from the live system on every run, don't hardcode; (2) latent CRLF bugs hide in (grep -c ...) on Windows — fix pattern is `tr -d '\n
 '` + `${var:-0}`, codify as a Windows-safe helper; (3) negative-test every new probe with synthetic drift injection — HERMES_BUNDLE_URL_OVERRIDE=https://mehyar.us/assets/main-NONEXISTENT.js is Section O's round-trip; (4) O = origin URL preserves A-N mnemonics; (5) parallel-tick working-tree WIP is ship-the-WIP-eligible at first sighting, regardless of scheduled cadence.

2026-07-10 · turn-053 — probe-section-O CRLF-safe count fix: applied the canonical `head -1 | tr -d ' \t\r\n'` + `${var:-0}` pattern (which turn-052 baked into J) to Section O's step 5 SKIP_LITERAL_COUNT. The original O probe had `grep -c -F -- ... || echo 0` (the bug-class variant: dead `|| echo` fallback because grep exits 0 on count=0, plus `0\n0` length-3 var instead of `0` length-1, breaking `[ ... -lt 1 ]` with `integer expression expected`). Three lessons: (1) `|| echo 0` after `grep -c` is almost never the right defensive pattern — grep exits 0 on count=0 (success with no matches), the fallback never fires; the right defense is `${var:-0}` AFTER the trailing-newline strip, not a fallback that doesn't fire; (2) the `3+ scripts exhibit the same fix pattern` threshold for Section P (CRLF-on-Windows hardening rubric) is the right gate — 2 fixed instances (J + O) is below the threshold; opening a rubric section on 2 known bugs is premature; (3) the negative-test pattern (3-case probe: empty file -> count=0 caught, file without literal -> count=0 caught, real bundle -> count=1 caught) is the only verification that proves the count=0 path actually fires a FAIL instead of `integer expression expected`. Same pattern turn-042 baked into K, turn-046 into N, turn-052 into O. The fix is transparent on the happy path (probe still PASSes main-1wxJxxD5.js, exit 0) — the integrity value is in the failure mode being clean when it ever fires (which would only happen on a future bundle-roll that drops the skip-link literal). State of the world: 2 of 3 hot-list items still founder-blocked (Section G pricing + W5-PERSUADE persuasion); loop continues to ship docs-only probe-hygiene ticks while waiting.

(prior notes in git log `git log --all --oneline -- .hermes/audit/learned.md` and .hermes/audit/turn-NNN.md history)

2026-07-10 · turn-061 — Section J `$330`-literal removal: redundant probe FAILs for the same drift class are a rubric-drift sub-pattern. Section J was FAILing every tick since turn-037 (when the founder-blocked PRICING-LADDER-DRIFT doc opened) because it asserted `pricing-section.tsx :: $330` as a required literal. Under the current $150/$250 ladder the assertion is always false; under Option A or C from the PRICING-LADDER doc it would become true. Section G already surfaces the same drift via `G FAIL: tier-1=$150 intake=$330` with the full 4-row drift table. Two FAILs for the same drift = redundancy, not signal. Fix: drop the `$330` PROBES entry (probe now runs 8 checks, was 9); document the founder-blocked class in a `#` comment block above the array; re-add once Option A/B/C ships and src/ catches up. Three sub-lessons: (1) bash `#` comments don't work inside `PROBES=( ... )` array assignments — the parser would treat them as array elements. The comment block lives above the assignment. (2) When a probe asserts a literal that depends on a founder-decision-blocked change, the rubric drift class is "Probe asserts a literal the founder hasn't committed" — bake it as a failure-mode catalog row, not a one-off turn note. (3) The cheapest recurring check after a docs-only ship is to verify the rubric still points at the right invariants: probe-script + rubric-doc + audit-record must all carry the same change, or the next reader will rebuild the wrong understanding. State of the world unchanged otherwise: Section G pricing drift still founder-blocked (turn-037 options A/B/C awaiting reply); W5-PERSUADE still ready (founder reply needed on docs/PERSUASION-PROPOSAL.md); Section P parked at 2-of-3 on count-strip class; no live deploy this tick (CF Pages stays on turn-060 main-Dsuzx-uI.js).

2026-07-18 · turn-062 — Section H probe JSX tag-quote alphabet drift: probe-rubric drift is a probe-side bug, not a source-side bug. Live bundle rolled to main-p303-96M.js (666,443 bytes; +16% size) between turn-061 and turn-062; the bundle minifier switched JSX-emitted tag literals from backtick `` ` `` to double-quote `"`. Old regex `(tag)\`` returned 0; new regex `jsx\("(tag)"\)` returns 49. The drop from 61 (old) to 49 (new) is correct-by-design — the old regex was over-counting via prop-value-style backtick literals that incidentally looked like tag names. Three sub-lessons: (1) when a single probe starts FAILing while the rest of the rubric stays green, suspect probe alphabet drift, not source regression; diff the probe regex against the bundle's actual byte-shape in <10s. (2) `grep -oE '"(tag)"' bundle | wc -l` is the universal alias-tolerant probe — works regardless of quote style (backtick, single, double, none) — start broad and narrow once you've confirmed the current alphabet. (3) A probe-alphabet drift that's been silent for N turns means the previous output numbers were lucky by coincidence; the new numbers are correct by design — don't try to restore the old count, accept the new correct one. Section H's failure-mode catalog grew a 7th row documenting the class. The 9-section regression sweep is now self-healing for this class: any future bundle-roll that flips the JSX-emit quote style trips Section H, and the loop catches it on the next LOOP-BOOT tick (≤15min cadence). State of the world unchanged otherwise: Section G pricing drift still founder-blocked; W5-PERSUADE still ready; other 8 rubric probes (J/K/L/M/N/O/Q) all green; Section P parked at 2-of-3 on count-strip class (this drift is probe-alphabet, not count-strip, so doesn't count toward Section P threshold).

2026-07-18 · turn-063 — Section H probe alias-renamed jsx() wrapper: the JSX-emit alphabet has TWO shape-dimensions the probe must track simultaneously. Dimension 1 = call form (bare-direct `jsx(tag)` vs alias-renamed `(0,X.jsx)(tag)` — minifier uses `(0, X.jsx)` to call jsx export without shadowing a local var). Dimension 2 = quote style (double-quote / backtick / none). Turn-062 caught dim-2 alphabet flip; turn-063 catches dim-1 call-form flip landing between turn-062 (`main-p303-96M.js`, bare-direct) and turn-063 (`main-D9Djrf2D.js`, alias-renamed). Clean fix is a python helper that unions both shapes × both quote styles in one regex — bash + grep can't handle that depth cleanly (alternation gets eaten by both bash and grep -oE). Three sub-lessons: (1) a probe whose regex is two-shapes × two-quote-styles deep is past the bash-grep point — move to python where regex literals are first-class. (2) negative-test the python helper with synthetic fixtures for ALL shape × quote combos + edge cases (empty / no-landmark) before shipping; the 5-case verification locks the contract so a future third shape trips the negative test first. (3) working-tree WIP from interrupted ticks is ship-the-WIP-eligible at first sighting (turn-050/051/052 reinforced) — the in-progress turn-063 python helper + modified probe were on disk, right move was to verify + ship, not start fresh. State unchanged otherwise: Section G pricing drift founder-blocked; W5-PERSUADE ready; 8 other rubric probes green (G expected FAIL); Section P parked at 2-of-3.
