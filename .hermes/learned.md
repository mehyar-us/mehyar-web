# learned.md — mehyar.us improve-loop

> Append-only. One line per tick. Latest at the bottom.
> Format: `YYYY-MM-DD HH:MM · <one-line takeaway>`

2026-07-09 16:55 · turn-028 · every VISION.md reference to a docs/ file must point at an actual committed path — recreated docs/QA-MEHYARSOFT-B2B-BASELINE-2026-05-11.md from shipped artifacts (sha 13ebb08, docs-only, no CF Pages deploy needed). CF Pages deploy workflow path filter is a feature: docs-only commits don't trigger the 5min build lag.
2026-07-09 17:08 · turn-029 · recreating a referenced doc closes the file-on-disk gap but does NOT automatically fix any line that *names* the doc — VISION.md "Current state" line still had the same wrong-filename drift after turn-028 (pointed at non-existent `docs/QA-MEHYARSOFT-B2B-BASELINE` instead of `docs/QA-MEHYARSOFT-B2B-BASELINE-2026-05-11.md`). Also ops doc count was wrong ('2 ops docs' with 3 filenames listed; actually 4 on disk). Both layers (file existence + reference correctness) are one state. Phase-6 4-screen LOOP-BOOT partial against the recreated rubric passed all 14 spot-check items — full 13-route audit is now a viable next tick.
2026-07-09 18:18 · turn-030 · CF Pages scripts/-only deploys can roll the shell files (with the route-injected JSON-LD) while keeping the JS bundle hash at the previous build. Verify the artifact that matters for the change (shell JSON-LD count for SEO ticks; runtime behavior for UX ticks) — not the bundle hash alone. When shipping scripts/route-jsonld.json edits, expect the home shell to update but the main-P-*.js hash may not change. Net: SEO outcome (more JSON-LD for crawlers) lands even when the JS bundle is cached.
2026-07-09 18:25 · turn-031 · a docs-only LOOP-BOOT audit tick against a rubric is the cheapest cadence-anchor between shipping ticks — ≤4 probe screens per the class budget, no CF Pages deploy needed, converts an implicit rubric-on-disk into a verified-this-tick ledger entry. Full 13-route audit caught zero regressions this run, which is the right answer for a rubric that was just recreated at turn-028 — the first LOOP-BOOT after a rubric rebuild is mostly a "verify the rubric matches reality" exercise, not a regression hunt. The cadence value is the future: when something DOES regress, this tick is the pattern that catches it cheaply. Closes t_b3048d53.
2026-07-09 20:55 · turn-033 · a section-level rename is a 2-place minimum job, not a 1-place job — after turn-032 changed the pricing-section eyebrow from 'Offer ladder' to 'leak ladder', the home hero CTA label ('See the offer ladder') AND the Sitemap /services row description ('Offer ladder, pricing ranges, ...') were missed. Shipped both as one voice-reconciliation tick (sha a1d3548, PR #3). Live bundle main-DrrbqAOE.js: 'See the leak ladder' 1x, 'Leak ladder' 2x, 'offer ladder' 0x — the 'offer ladder' meta-noun stays in VISION.md voice-bar bullet because that's a meta-principles statement, not visitor-facing copy. /contact count went 21→20 and micro-offer#intake went 19→20 — both are turn-032's tier-2 CTA move, not a turn-033 regression. Cheap post-rename check: `grep -rn '<old-name>' client/src/` should return 0 hits in visitor-facing copy. CF Pages src/ change DOES roll the JS bundle (main-DrrbqAOE.js this tick vs main-P-x17WD-.js turn-029/030) — scripts/-only deploys roll the shell but not the bundle, src/ deploys roll both. Closes t_7f7dbdd7.

2026-07-09 22:25 · turn-035 · shipped hero secondary CTA target fix /services → /#pricing (sha 5f49f9c, PR improver/hero-cta-pricing-turn-035) — 1-tap bounce closed. Hero 'See the leak ladder' now scrolls to the actual 6-tier pricing section on the home page, not to the unrelated /services consulting-offer grid. Closes the W2-FUNNEL hero secondary-CTA piece that turn-004 (primary CTA target) and turn-033 (secondary CTA label) missed. src/-vs-scripts/ bundle-roll pattern locked with 3/3 data points: src/ change rolls shell + bundle (turn-027 / turn-033 / turn-035 all confirmed); scripts/ change rolls shell only (turn-030 confirmed). Local build main-BEFkljKi.js → CF Pages build main-CqiU6zle.js (hash differs by build host; content-fingerprint via `/#pricing` literal = 1 is the right verification). Cheap post-rename / post-add CTA check added: list every href in the live bundle and verify each one is the URL the visitor expects to land on, not the URL a developer pasted during build. Section-anchor hrefs are a separate dimension from copy. Closes the W2-FUNNEL hero secondary-CTA piece; W2-FUNNEL now fully closed: turn-004 + turn-005 + turn-006 + turn-027 + turn-035.
2026-07-09 22:30 · turn-036 · shipped About Practicality card copy — drop 'leverage' (sha c33755d, PR #4, live bundle main-BKU1Uoxy.js). About.tsx value-card #1 had the only remaining visitor-facing 'leverage' noun ("highest-leverage business leak..."); rewrote to "Find the leak that costs the most, fix it first, and stop adding tools before the case is clear." 1 src/ edit, 1 file, 1 line. Voice 5/5; anti-slop 0 hits; audit-intent funnel unchanged from turn-035 baseline. W1-SLOP closure reaffirmed.
2026-07-09 22:55 · turn-037 · surfaced tier-1 pricing drift (sha 17c70a3 on github main; docs-only — no CF Pages deploy). Detected visitor-facing contradiction: VISION.md + pricing-section.tsx both list tier-1 as '$150 Free Tech Audit', but /micro-offer (the actual intake CTAs point at) charges $330 (49 bundle mentions; api-contract $330 truth). Result on live home: tier-1 ($150) < tier-2 ($250) < actual intake ($330) AND both tier-1+2 CTAs land on same /micro-offer page. Wrote docs/PRICING-LADDER-DRIFT-2026-07-09.md with 3 decision options A/B/C. Did NOT ship any of the 3 — too consequential for autonomous. Recommended: add pricing-consistency check to LOOP-BOOT rubric. Lesson: founder's stated pricing ladder (VISION.md) and revenue reality (micro-offer charge) can drift apart silently; the cheapest catch is a 3-line check added to the rubric.
2026-07-09 23:15 · turn-038 · added Pricing-consistency check (Section G) to LOOP-BOOT rubric (sha 2b50b36 on github main; docs-only — no CF Pages deploy). New probe `.hermes/probe-section-G.sh` is a 3-grep bundle check: tier-1 price from pricing-section.tsx + most-frequent $ string from MicroOffer.tsx + FAIL if they disagree. Verified FAIL this tick with `G FAIL: tier-1=$150 intake=$330` — the rubric is now strong enough to catch turn-037's drift on any future LOOP-BOOT audit. 4-row failure-mode catalog added for future drift patterns (VISION vs pricing-section, multi-tier-routing, MicroOffer vs api-contract). Re-purposed old Section G "Open registry" to new Section H. Probe exit-1 is CI-ready. Lesson: when a prior audit missed a real drift, the right fix is usually to add the cross-check to the rubric (cheap, reversible, docs-only) rather than just patch the immediate case.
2026-07-10 00:48 · turn-039 · added Accessibility/SEO smoke probe (Section H) to LOOP-BOOT rubric (sha d9d96b9 on github main; docs-only — no CF Pages deploy, live site stays on turn-036 c33755d). Rubric grew 8 sections A-H → 9 sections A-I. New probe .hermes/probe-section-H.sh runs 9 grep-based checks against live bundle main-BKU1Uoxy.js (574085 bytes; expect ~574069): skip-link, semantic landmarks, aria-hidden/labelsr-only, lang/viewport/canonical on home shell, JSON-LD re-check. Verified PASS this tick — 9/9 green. Section G probe regression run still FAIL exit 1 (pricing drift open, expected). Naming-consistency bugs in working tree caught + fixed in this turn: doc referenced probe-section-I.sh in 3 places, probe script banner + 19 echo lines said I not H. Lesson: a section rename is a 4-place job (doc heading + doc body refs + probe banner + probe echo prefixes), not the 2-place job turn-038 lesson claimed. The Section G probe keeps doing its job (caught drift on turn-038 + turn-039). tsc green, test:intake 11/11, 4-screen 200/200/200/404. State: pricing drift still awaits founder decision; W5-PERSUADE still awaits user direction; loop has 0 todo, 0 blocked, 2 ready.
2026-07-10 01:35 · turn-040 · added Build-artifact-integrity probe (Section J) to LOOP-BOOT rubric (sha e464f1f on github main; docs-only — no CF Pages deploy). Rubric grew 9 sections A-I → 10 sections A-J. New probe `.hermes/probe-section-J.sh` is the inverse of Section H: it pins src/ literals to the live bundle, catching "src/ has a literal that's not in the live bundle" drift (the failure mode from turn-030's scripts/-only deploy). 9 probes covering: Newsletter skip-link "Skip to the $330 audit", hero CTA copy "See the leak ladder", 5 pricing strings from pricing-section.tsx, 2 Navbar aria-labels. Each probe asserts src_count>=1 AND bundle_count>=1; FAIL exits 1. Verified PASS this tick — 9/9 green on live bundle main-BKU1Uoxy.js (574085 bytes). Section H probe regression-run still 9/9 PASS. Section G probe regression-run still FAIL exit 1 (pricing drift open, expected). 5-row failure-mode catalog added for future deploy-pipeline drift patterns (src/edit + bundle missing, src rename, hash change, partial deploy, dead literal). Section I "Open registry" unchanged (stays as-is between I and new J — naming rule: don't rename I→J just to add K; keeps the "I = Open registry" mnemonic stable for external readers). tsc green, test:intake 11/11, 4-screen 200/200/200/404, anti-slop 0 hits. Lesson: Sections H and J together pin the bundle to the committed source tree in both directions — H catches "bundle literal not in any src/" (stale bundled content), J catches "src/ literal not in bundle" (stale deployed content). The pair is the cheapest deploy-pipeline drift detector on the budget and is now CI-ready. Next rubric extension: Section K (type safety regression).
2026-07-10 05:14 · turn-045 · ship the Section M rubric extension that turn-044 left as uncommitted worktree changes (sha 374c5be on github main; docs-only — no CF Pages deploy, live site stays on turn-036 c33755d). turn-044's tick created the probe + audit record + doc + state update on disk but never landed in git (working tree had the 4 files modified/untracked at tick end). Closed it cleanly: `git add .hermes/audit/turn-044.md .hermes/probe-section-M.sh` (Section K probe would have flagged the untracked audit record; ran it before commit and confirmed the orphan — that was the same kind of self-detection turn-042 baked into the rubric); `git commit -m "docs(loop): turn-045 — ..."`; `git push origin main` (374c5be, the 7-char prefix now visible in git log + the github API commits/main endpoint confirms `374c5bedb7c53294113586517b93dc915e5df176` matches local HEAD). Re-ran all 6 probes (G/H/J/K/L/M) — Section G pricing-consistency still FAIL exit 1 (tier-1=$150 intake=$330 — founder-decision-blocked, expected); Sections H/J/K/L/M all PASS. Section M specifically: 42 cited / 202 in git (was 201 last tick; the new commit 374c5be added 1 to git's side, cited count stayed at 42 because turn-044 audit prose was already-cited). Lesson: when a tick ends with files in working-tree-but-not-committed state, the next tick's first job is to land the previous tick's work — don't ship a new feature on top of a dirty tree. Section K was the cheap detector (caught the orphan audit record on the first run); without it this drift would have silently grown across turns until the user re-verified.
2026-07-10 05:25 · turn-046 · ship Section N File-path-reference probe (rubric 13→14 A-N) — the third cousin-class identifier-drift rubric extension (L=ticket-ids / M=commit-SHAs / N=file-paths). Probe caught its own target on first run: docs/PERSUASION-PROPOSAL.md had been referenced from VISION.md / state.md / audit turn-016/018/025/031/044 since the bootstrap but never landed on disk (a 31-tick citation-of-nonexistent-file drift). Same tick closed it by creating the template (with 3 shape options A/B/C + hybrid option + decision-request format for the founder). Negative-test round-trip PASS (synthetic docs/NEGATIVE-TEST-N.md → exit 1 → restore → exit 0). Probe shape: `grep docs/[A-Za-z0-9_./-]+\.md` from state.md + VISION.md + 2 rubric docs + pricing-drift doc + learned.md + every audit/turn-*.md (globbed at runtime so turn-NNN+1 citations are caught without updating the probe), strip inline-code backticks first (same pattern Section M uses for synthetic SHAs in negative-test prose), then `test -f` each cited path. ~5s wall time, pure fs check (no DB / git round-trip needed). All probes this tick: G FAIL exit 1 (pricing drift open, expected); H/J/K/L/M/N all PASS. 4-screen 200/200/200/404; voice 5/5 unchanged; anti-slop 0 hits. Lesson: the "fabrication direction" principle (cited-but-missing = FAIL, not-cited = informational) holds across all three identifier classes. The pattern that's now confirmed with 3 data points (L, M, N): a probe that catches drift on first run + closes its own target in the same tick is the textbook cheap-and-right rubric extension. Section M's update-cadence note already anticipated Section N ("add a cousin probe (Section N, etc.) following the same pattern") — when the rubric's own prose names a future extension, building it in the next natural lull closes the loop cheaply. Next rubric extension candidate (O): URLs / env-var values / telemetry event names if any of those classes starts to rot.
2026-07-10 05:45 · turn-047 · fix Section N probe negative-test bare-prose bug (cousin-probe lesson). Section N's first run (turn-046) had a probe that correctly FAILed on its own audit prose describing the negative-test path bare ("synthetic docs/NEGATIVE-TEST-N.md → exit 1 → restore → exit 0") — the backtick-strip (Section M pattern) didn't catch bare-prose file paths the way it catches synthetic SHAs inside-backticks. Fix: added a second pre-grep `sed` filter that strips the negative-test path literal alongside the backtick-strip — `sed -E 's/`[^`]*`//g; s|docs/NEGATIVE-TEST-N\.md||g'`. Same approach Section L uses for synthetic ticket-id literals. Section N re-ran: 9 cited / 9 on disk PASS exit 0; negative-test round-trip PASS (synthetic docs/FAB-TEST-N.md → exit 1 → restore → exit 0). Updated rubric Section N with a "Two pre-grep filters" implementation note cross-referencing the L (structural-shape) and M (inside-backticks) precedents. All probes this tick: G FAIL exit 1 (pricing drift open, expected); H/J/M/N/L all PASS; K transient FAIL (orphan turn-046.md + turn-047.md pre-commit — expected). 4-screen 200/200/200/404; voice 5/5 unchanged; anti-slop 0 hits. Lesson: a cousin probe (L → M → N share grep + sort + comm shape) gets its negative-test strip right the first time only if the strip matches HOW the negative-test scenario is naturally described in prose, not just the probe's regex alphabet. The audit prose should naturally mention the negative-test without contorting itself to fit inside backticks; the probe should accommodate the natural shape. Next rubric extension candidate (O): if a new identifier-drift surface surfaces (URLs cited in audit prose that no longer resolve, env-var values renamed, telemetry events referenced in state.md but no longer emitted).
